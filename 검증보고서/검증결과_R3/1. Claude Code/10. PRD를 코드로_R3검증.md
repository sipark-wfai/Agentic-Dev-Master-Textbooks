# 10. PRD를 코드로 — 3차 할루시네이션 검증 보고서 (R3)

**검증일**: 2026-06-05
**검증 라운드**: Round 3
**원본 파일**: 10. PRD를 코드로.md
**R2 수정 적용 상태**: 해당없음 (R2에서 수정본 미생성)
**전체 할루시네이션 위험도**: 낮음

## R2 잔존 이슈 재확인
| R2 지적 항목 | 수정 여부 | 비고 |
|------------|---------|-----|
| --skip-git 옵션 지원 불확실 | 미수정 | 541행에 `--skip-git` 잔존 |
| pnpm --parallel 방식 | 미수정 | 147행에 `--parallel` 잔존 |
| JWT_REFRESH_EXPIRES_IN 30d (08편과 불일치) | 미수정 | 283행에 30d 잔존. 08편은 7일/30일 불일치 |

## 잔존/신규 발견 오류

### 🔴 심각
없음

### 🟡 주의
| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| --skip-git 옵션 | `npx @nestjs/cli new backend --package-manager pnpm --skip-git` | NestJS CLI v10+에서 `--skip-git` 옵션 지원 여부 불확실. `--no-git` 또는 단순히 없어도 됨 | 541행 |
| pnpm --parallel | `pnpm --parallel --filter './apps/*' dev` | `--parallel`은 pnpm 명령어에서 `-r`(recursive)과 다른 동작. 필터와 함께 사용 시 동작 방식이 pnpm 버전에 따라 다를 수 있음 | 147행 |
| RefreshToken 만료 시간 편간 불일치 | `JWT_REFRESH_EXPIRES_IN=30d` | 08편의 비기능 요구사항에서 7일로 명시된 것과 불일치. 10편은 30일 사용 | 283행, 645행 |

### 🟢 확인됨 (정확)
- pnpm-workspace.yaml 구조 정확
- NestJS AuthService의 bcrypt 사용 방식 정확
- JWT accessToken + refreshToken 이중 토큰 발급 로직 정확
- ConflictException, UnauthorizedException 사용 정확
- Prisma findUnique with deletedAt: null 패턴 정확
- @nestjs/passport, passport-jwt 패키지 정확
- ConfigService를 통한 환경변수 읽기 정확

## R2 대비 개선도
- R2 WHI: 0.13 (낮음)
- R3 WHI: 0.09 (낮음)
- 개선율: 31%

## 전체 신뢰도 점수 (R3)
- **점수**: 8.8/10
- **근거**: 실제 구현 코드가 기술적으로 정확. --skip-git 옵션과 편간 refreshToken 만료 불일치가 사소한 주의 사항

## 수정본 생성 여부
- 생성함 (🟡 주의 3건 잔존)
