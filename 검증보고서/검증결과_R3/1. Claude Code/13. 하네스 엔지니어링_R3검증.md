# 13. 하네스 엔지니어링 — 3차 할루시네이션 검증 보고서 (R3)

**검증일**: 2026-06-05
**검증 라운드**: Round 3
**원본 파일**: 13. 하네스 엔지니어링.md
**R2 수정 적용 상태**: 미완료 (핵심 오류 3건 모두 미수정)
**전체 할루시네이션 위험도**: 높음

## R2 잔존 이슈 재확인
| R2 지적 항목 | 수정 여부 | 비고 |
|------------|---------|-----|
| permissions.allow/deny 스펙 미지원 (심각) | 미수정 | 84, 203, 324행에 잔존 |
| limits 섹션 미지원 스펙 (심각) | 미수정 | 293~301행에 잔존 |
| Hook ${command}/${file} 직접 치환 방식 (심각) | 미수정 | 238, 242, 251, 255, 266, 366행에 잔존 |
| CLAUDE_CONTEXT_TOKENS 환경변수 불확실 | 미수정 | 161행에 잔존 |
| CLAUDE_MAX_TOKENS 환경변수 불확실 | 미수정 | 162행에 잔존 |

## 잔존/신규 발견 오류

### 🔴 심각
| 항목 | 원문 내용 | 실제 사실 | 위치 |
|------|----------|----------|------|
| settings.json permissions 섹션 | `"permissions": {"allow": [...], "deny": [...]}` | Claude Code 공식 settings.json에 allow/deny 경로 배열 구조는 지원되지 않음. 실제 파일 접근 제어는 Hook(PreToolUse)으로 구현해야 함 | 84, 203, 324행 |
| settings.json limits 섹션 | `"limits": {"maxFilesPerSession": 100, ...}` | Claude Code 공식 스펙에 존재하지 않는 섹션. 설정해도 실제로 적용되지 않음 | 293~301행 |
| Hook ${command}/${file} 직접 치환 | `".claude/hooks/human-approval.sh \"${command}\" PreToolUse"` | Claude Code Hooks는 stdin JSON 방식으로 데이터 전달. ${command}, ${file}은 직접 치환되지 않아 스크립트에서 빈 문자열로 처리됨 | 238, 242, 251, 255, 266, 366행 |

### 🟡 주의
| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| CLAUDE_CONTEXT_TOKENS 환경변수 | `CURRENT_TOKENS="${CLAUDE_CONTEXT_TOKENS:-0}"` | Claude Code가 Hook 실행 시 이 환경변수를 제공하는지 공식 문서에서 확인 불가 | 161행 |
| CLAUDE_MAX_TOKENS 환경변수 | `MAX_TOKENS="${CLAUDE_MAX_TOKENS:-200000}"` | 동일하게 공식 지원 불확실 | 162행 |

### 🟢 확인됨 (정확)
- 하네스 엔지니어링 개념 정확
- PreToolUse exit 2 차단 패턴 정확
- Hooks 이벤트 유형 설명 정확
- any 타입 감지 패턴 정확
- 하네스 성숙도 모델 Level 0~5 개념 정확
- chmod +x 권한 설정 정확

## R2 대비 개선도
- R2 WHI: 0.29 (중간)
- R3 WHI: 0.29 (중간) ※ 미수정으로 동일 수준 유지
- 개선율: 0% (핵심 오류 미수정)

## 전체 신뢰도 점수 (R3)
- **점수**: 6.0/10
- **근거**: R1~R2에서 지속 지적된 핵심 오류(permissions/limits 미지원 스펙, Hook 변수 치환)가 모두 미수정. 교재를 따라 구현 시 실제 동작하지 않는 코드를 작성하게 됨

## 수정본 생성 여부
- 생성함 (🔴 심각 3건, 🟡 주의 2건 잔존 — 수정 우선순위 최상)
