# 16. 트러블슈팅 완전 정복 & 고수되기 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 16. 트러블슈팅 완전 정복 & 고수되기.md
**R1 수정 적용 상태**: 완료
**전체 할루시네이션 위험도**: 중간

## R1 수정 확인 결과

| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| CLI 명령어 ag → agy 수정 | 적용됨 | 교재 전체에서 `agy` 사용 확인 (agy status, agy logs, agy agent 등) |
| 로그 파일 경로 명확화 | 적용됨 | `agy logs --level error --tail 50` 명령어 기반 로그 조회로 변경, 특정 파일 경로 직접 제시 없음 |

## 잔존/신규 발견 오류

### 🔴 심각

| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| 스킬 설치 경로 | `cd ~/.gemini/skills/image-optimizer` | 연구자료 기준 스킬 경로는 `.agents/skills/` (저장소 로컬). `~/.gemini/` 경로는 Gemini CLI 시대의 전역 설정 위치로, Antigravity 2.0 공식 경로와 불일치 가능성 높음 | 오류 6 (스킬 실행 오류) |
| 로드맵 CLI 표현 잔존 | "GitHub Actions + ag CLI 자동화" | 공식 CLI 명령어는 `agy`. `ag`는 R1에서 지적된 오류 표현이 Level 2 로드맵에 잔존 | §6.1 Level 2 로드맵 |

### 🟡 주의

| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| 샌드박스 Docker 이미지 경로 | `docker pull gcr.io/antigravity-sandbox/alpine-base:2.0` | 이미지 경로 공식 미확인. 교재가 구체적인 GCR 경로를 단정적으로 제시하나 실제 이미지명이 다를 수 있음 | 오류 4 (샌드박스 초기화 실패) |
| Daemon 포트 번호 | `"antigravity.daemon.port": 7432` | 연구자료에서 포트 번호 공식 확인 불가. 교재가 구체적인 포트를 단정적으로 제시 | 오류 3 (IDE-Desktop App gRPC 연결 실패) |
| Proposal CLI 필터 문법 | `agy proposal accept --filter "testsPassed=true"` | 필터 표현식 문법 공식 미확인. 개념적으로 타당하나 실제 CLI 옵션 미확인 | 오류 11 (Proposal 카드 대량 누적) |
| 공식 문서 URL | `https://ai.google.dev/antigravity/docs` | URL 형식은 타당하나 정확한 경로 공식 미확인 | §6.3 커뮤니티 및 자료 |
| 비용 시뮬레이션 수치 | 마일스톤별 예상 비용 ($0.50 ~ $4.00) | 실제 Gemini 3.5 Flash API 단가에 따라 변동 가능. 예시임을 명시하면 충분 | §4.2 비용 시뮬레이션 |

### 🟢 확인됨 (정확)

- `agy` CLI 명령어 전체적으로 올바르게 사용 (agy status, agy logs, agy agent, agy mcp, agy cost, agy daemon, agy sandbox, agy timemachine, agy skill, agy proposal)
- 오류 유형 5단계 분류(Level 1~5) 방법론 정확
- Google AI Studio API 키 URL (`https://aistudio.google.com/apikey`) 정확
- /compact 컨텍스트 압축 기능 공식 확인
- 팀 협업 공유 파일 분리 (.agents/config.json, .agents/mcp.json, GEMINI.md Git 커밋 / .agents/local.json Git 제외) 타당
- 고수 작업 루틴(세션 시작·마일스톤·하루 마무리) 실용적이고 정확
- Git 기반 복구 절차 정확 (git checkout, git reset, git revert 사용법)
- SvelteKit SSR 브라우저 가드 (`onMount`, `typeof document !== 'undefined'`) 정확
- NestJS DI 오류 해결법 정확 (PrismaModule imports 추가)
- Prisma 마이그레이션 명령어 정확
- CORS 설정 (`app.enableCors`) 정확
- Docker 포트 점유 확인 (`lsof -i :포트`) 정확
- multiAgentGate 개념 타당 (공식 병렬 에이전트 지원 기반)
- autoCompact 설정 개념 타당

## R1 대비 개선도

- R1 WHI: 0.38 (심각 1×1.0 + 주의 1×0.3 = 1.3 / 검증항목 약 34개)
- R2 WHI: 0.20 (심각 2×1.0 + 주의 5×0.3 = 3.5 / 검증항목 약 44개)
- 개선율: R1 핵심 오류(ag CLI 전반)는 대부분 수정됨. 단, 로드맵 섹션 `ag` 잔존 및 스킬 경로 오류 신규 발견. 전반적인 신뢰도 향상.

> **WHI 계산**: 심각 2항목(×1.0=2.0) + 주의 5항목(×0.3=1.5) = 3.5 / 전체 검증 항목 44개 ≈ **0.20** (낮음~중간 경계)

## 전체 신뢰도 점수 (R2)

- **점수**: 7.5/10
- **근거**: R1에서 지적된 CLI 명령어 오류는 전체적으로 수정되었으며, 트러블슈팅 방법론·Git 복구·SvelteKit/NestJS/Prisma 코드 예시 등 기술적 내용의 정확도가 높다. 다만 스킬 설치 경로(`~/.gemini/skills/`)가 Antigravity 2.0 기준으로 불일치 가능성이 있고, 로드맵 섹션에 `ag CLI` 표현이 잔존하며, Docker 이미지 경로·데몬 포트 번호가 공식 미확인 상태로 남아 감점 요인이 있다.
