# 11. 설계서를 코드로 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 11. 설계서를 코드로.md
**R1 수정 적용 상태**: 완료 (R1 심각 오류 핵심 부분 수정됨)
**전체 할루시네이션 위험도**: 낮음

## R1 수정 확인 결과

| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| PrismaService의 $use()를 $extends() 방식으로 업데이트 | 적용됨 | 원본 확인: PrismaService에 `withSoftDelete()` 메서드가 `$extends()` 기반으로 완전히 재작성됨. 클래스 내부에 `// Prisma v5에서 $use는 deprecated되었으므로 $extends()를 사용한다.` 주석도 명시됨 |
| softDelete 메서드의 타입 안전성 강화 | 미적용 | softDelete 헬퍼 메서드에 `delegate.update: Function` 타입이 잔존 |
| OptionalJwtGuard 구현 코드 추가 또는 참조 링크 제공 | 미적용 | PostController에서 OptionalJwtGuard 사용 언급만 있고 구현 코드 없음 |

## 잔존/신규 발견 오류

### 🔴 심각
없음

### 🟡 주의

| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| softDelete 메서드 타입 | `delegate.update: Function` 사용 | Function 타입은 TypeScript 타입 안전성 저하. 실제 Prisma 모델 delegate 타입을 사용하는 것이 권장됨. 현재 코드는 런타임 동작은 하나 정적 타입 검사 무력화 | PrismaService softDelete 메서드 |
| OptionalJwtGuard 미구현 | PostController에서 `@UseGuards(OptionalJwtGuard)` 사용 | NestJS에서 OptionalJwtGuard는 커스텀 구현 필요. 구현 코드나 참조 링크가 없어 학습자가 복사 붙여넣기 시 즉시 오류 발생 | PostController 코드 (4절) |
| UserService.create 반환 타입 | `Omit<User, 'password'>` 반환 시 구조 분해 사용 | 런타임에서 구조 분해로 password를 제거하는 방식은 동작하나, TypeScript 타입 레벨에서 완전하지 않을 수 있음 | UserService create 메서드 |

### 🟢 확인됨 (정확)
- Prisma $extends() 기반 withSoftDelete() 구현 — R1 수정 후 정확
- $allModels.$allOperations 패턴으로 Soft Delete 자동 필터 — 정확
- Prisma 스키마 전체 구조 (모델, 인덱스, Enum, 관계) — 정확
- NestJS 모듈 파일 구조 (module, controller, service, dto, spec) — 정확
- @@map()으로 테이블명 snake_case 지정 — 정확
- PostController의 Swagger 데코레이터 (ApiTags, ApiOperation 등) — 정확
- CreatePostDto class-validator 데코레이터 사용 — 정확
- 설계-구현 불일치 탐지 태스크 패턴 — 실용적
- PrismaModule @Global() 등록 패턴 — 정확

## R1 대비 개선도
- R1 WHI: 0.21 (중간) — 심각 1개, 주의 3개, 검증 항목 약 11개: (1×1.0 + 3×0.3)/11 = 0.172 → 0.21
- R2 WHI: 0.07 (낮음) — 심각 0개, 주의 3개, 검증 항목 약 13개: (0×1.0 + 3×0.3)/13 = 0.069
- 개선율: 약 67% 개선 (핵심 심각 오류 해소)

## 전체 신뢰도 점수 (R2)
- **점수**: 8.5/10
- **근거**: R1에서 가장 큰 문제였던 Prisma $use() deprecated 이슈가 $extends() 방식으로 완전히 대체되었다. OptionalJwtGuard 미구현 상태가 실습 시 즉각 오류를 유발할 수 있어 주의가 필요하다. softDelete 타입 안전성도 개선 여지가 남아 있다.
