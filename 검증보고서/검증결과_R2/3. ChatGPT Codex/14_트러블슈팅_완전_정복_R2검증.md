# 14. 트러블슈팅 완전 정복 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 14. 트러블슈팅 완전 정복.md
**R1 수정 적용 상태**: 미완료 (R1 권고사항 미반영, 위험도 낮음)
**전체 할루시네이션 위험도**: 낮음

## R1 수정 확인 결과

| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| DI 오류 해결 태스크에 "다른 모듈의 PrismaModule imports 제거" 지시 주의 문구 추가 | 미적용 | 원문 그대로 유지 |
| git push --force-with-lease에 "팀 공유 브랜치에서는 주의" 설명 추가 | 미적용 | 원문 그대로 유지 |

## 잔존/신규 발견 오류

### 🔴 심각
없음

### 🟡 주의

| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| @Global() 후 PrismaModule imports 제거 지시 | "@Global() 추가 후 다른 모듈의 imports: [PrismaModule] 모두 제거" | Codex에 이 지시를 그대로 전달하면 의도치 않게 여러 모듈의 imports 배열을 수정하여 부작용 발생 가능. "필요 시 선택적으로" 표현 권고 | 오류 10 DI 오류 해결 태스크 |
| git push --force-with-lease | 정확한 명령어이나 공동 작업 브랜치에서 위험 | 팀 공유 브랜치(main, develop 등)에서 실수로 사용 시 다른 팀원의 커밋을 덮어쓸 수 있음. 상황 설명 보완 없음 | 오류 14 PR 충돌 해결 |
| PostgreSQL 사용자 생성 heredoc | `psql -U postgres << 'EOF' ... EOF` | 일부 셸 환경(특히 Windows Git Bash)에서 heredoc이 제대로 처리되지 않을 수 있음 | 오류 19 PostgreSQL 연결 오류 해결 |

### 🟢 확인됨 (정확)
- GitHub OAuth 재연결 절차 — 정확
- AGENTS.md 파일명 오타 체크 방법 (agents.md, AGENT.md 탐지) — 정확
- TypeScript strict 모드 undefined 처리 (ConfigService 활용) — 정확
- Prisma 마이그레이션 충돌 방지 원칙 (순차 실행) — 정확
- 브랜치 보호 규칙 설정 방법 (PR Required approvals) — 정확
- SvelteKit SSR 브라우저 전용 코드 처리 ($app/environment browser) — 정확
- CORS 설정 (credentials: true 필수 조건) — 정확
- 환각 오류 해결법 (공식 문서 URL 태스크에 명시) — 실용적
- 20가지 오류 시나리오 전반적 정확도 — 높음

## R1 대비 개선도
- R1 WHI: 0.09 (낮음) — 심각 0개, 주의 3개, 검증 항목 약 10개
- R2 WHI: 0.09 (낮음) — 심각 0개, 주의 3개, 검증 항목 약 10개
- 개선율: 0% (수정 미적용, 동일 수준 유지)

## 전체 신뢰도 점수 (R2)
- **점수**: 8.5/10
- **근거**: 20가지 오류 시나리오와 해결법은 실용적이고 대부분 정확하다. R1 권고사항이 미적용되었으나 각 주의 항목의 위험도가 낮아 전체 신뢰도에 미치는 영향이 제한적이다. @Global() 후 imports 제거 지시에 주의 문구 추가가 실습 안전성을 높이는 데 필요하다.
