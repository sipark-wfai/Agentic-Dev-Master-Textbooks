# 05. MCP 완벽 가이드 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 05. MCP 완벽 가이드.md
**R1 수정 적용 상태**: 완료
**전체 할루시네이션 위험도**: 낮음

## R1 수정 확인 결과
| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| MCP 경로 혼재 (`claude_desktop_config.json` vs `.claude/settings.json`) 정리 | 적용됨 | 3.1절에서 Claude Code CLI는 `.claude/settings.json`, Claude Desktop은 `claude_desktop_config.json`으로 명확히 구분 |
| 두 파일의 용도 차이 명시 | 적용됨 | "이 교재의 모든 MCP 설정 예시는 Claude Code CLI 기준으로 `.claude/settings.json`을 사용한다" 명기 |

## 잔존/신규 발견 오류

### 🔴 심각
없음

### 🟡 주의
| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| `@modelcontextprotocol/server-postgres` 패키지 | npx -y로 자동 설치 | 실제로 존재하는 패키지인지, 패키지명이 정확한지 확인 필요. npm에서 `@modelcontextprotocol/server-postgres` 확인 필요 | 4.1절 |
| `@modelcontextprotocol/server-git` | 동일 | 동일한 검증 필요 | 4.2절 |
| `@modelcontextprotocol/server-filesystem` | 동일 | 동일한 검증 필요 | 4.3절 |
| `@modelcontextprotocol/server-brave-search` | 동일 | 동일한 검증 필요 | 4.4절 |
| Brave Search API URL | https://api.search.brave.com | 실제 Brave Search API 가입 URL이 다를 수 있음 | 4.4절 |
| MCP SDK import 경로 | `@modelcontextprotocol/sdk/server/index.js`, `@modelcontextprotocol/sdk/server/stdio.js` | .js 확장자 명시가 필요한지 패키지 버전에 따라 다름 | 7.1절 |
| `CLAUDE_LOG_LEVEL=debug claude` | 환경변수로 로그 수준 제어 | 실제 Claude Code에서 이 환경변수 지원 여부 불명확 | 6.3절 |

### 🟢 확인됨 (정확)
- MCP 개념 설명 (stdio vs SSE 전송 방식) 정확
- `.claude/settings.json` 기본 JSON 구조 정확
- Docker Compose healthcheck 패턴 정확
- Prisma 스키마 예시 정확
- MCP 아키텍처 (Client → Host → Server → 실제 시스템) 정확

## R1 대비 개선도
- R1 WHI: 0.32 (중간)
- R2 WHI: 0.18 (낮음)
- 개선율: 44%

## 전체 신뢰도 점수 (R2)
- **점수**: 8.2/10
- **근거**: 경로 혼재 문제가 잘 해결됐고, 일부 MCP 서버 패키지명 및 환경변수 지원 여부가 미확인
