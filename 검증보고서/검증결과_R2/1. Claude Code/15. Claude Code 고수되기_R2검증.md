# 15. Claude Code 고수되기 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 15. Claude Code 고수되기.md
**R1 수정 적용 상태**: 일부완료
**전체 할루시네이션 위험도**: 낮음

## R1 수정 확인 결과
| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| 메타 헤더 "Calude → Claude" 오타 수정 | 적용됨 | "Claude Sonnet 4.6"으로 정상 기재 |
| 메타 헤더 교재명 수정 (01편 제목 → 15편 제목) | 미적용 | 여전히 `1. Claude Code 완벽 입문 — 에이전트 퍼스트 패러다임과 에코시스템`으로 오기재 |
| `--headless` 옵션을 실제 동작 옵션으로 교체 | 미적용 | `claude --headless --print` 방식이 섹션 3, 4.1에서 동일하게 사용 |
| GitHub Actions Claude Code 인증 설정 방법 보완 | 미적용 | CI 환경 인증 방법에 대한 추가 설명 없음 |
| "Anthropic Agents SDK" 공식 명칭 수정 | 미적용 | 섹션 5.3 표에서 "Anthropic Agents SDK"로 동일 기재 |

## 잔존/신규 발견 오류

### 🔴 심각
| 항목 | 원문 내용 | 실제 사실 | 위치 |
|------|----------|----------|------|
| 교재명 오기재 | `교재 명: 1. Claude Code 완벽 입문 — 에이전트 퍼스트 패러다임과 에코시스템` | 15편의 교재명이 01편 제목으로 잘못 기재됨. "15. Claude Code 고수되기 — 실무 팁, 베스트 프랙티스, 그 다음 단계"가 맞음 | 메타 헤더 |
| `--headless` 미존재 옵션 반복 사용 | `claude --headless --print --model claude-haiku-4-5 ...` | Claude Code CLI에 `--headless` 옵션은 존재하지 않음. 04편에서도 동일하게 지적됨. GitHub Actions 워크플로우 및 PR 리뷰 스크립트에 영향을 미침 | 섹션 3(scripts/pr-review.sh), 섹션 4.1(GitHub Actions) |

### 🟡 주의
| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| 모델명 불확실성 | `claude-haiku-4-5`, `claude-sonnet-4-5`, `claude-opus-4-5` | 버전 번호 형식 일부 불확실. Anthropic 공식 API에서 `-4-5` 버전이 실제 배포되었는지 확인 필요. 모델 선택 전략 자체는 유효함 | 섹션 2.3, 4.1 |
| "Anthropic Agents SDK" 명칭 | `Anthropic Agents SDK` | 공식 SDK 명칭은 "Claude Agent SDK" 또는 Anthropic 공식 문서 기준 확인 필요. 잘못된 명칭으로 검색 시 혼동 유발 | 섹션 5.3 표 |
| GitHub Actions CI 인증 미설명 | `npm install -g @anthropic-ai/claude-code` 후 `ANTHROPIC_API_KEY` 환경변수만 설정 | CI 환경에서 `claude` CLI 실행 시 인증 방식, 비대화형 실행 설정 등에 대한 추가 안내 부족 | 섹션 4.1 |

### 🟢 확인됨 (정확)
- 협업 크레딧 "Claude Sonnet 4.6" 정상 기재
- `/cost`, `/compact` 사용 시점 가이드 정확
- `@파일경로` 컨텍스트 최소화 전략 정확
- 팀 CLAUDE.md Git 공유 방법 정확
- 역할별 CLAUDE.md 레이어 설계 정확
- GitHub Actions workflow 구조(on, jobs, steps) 정확
- `actions/checkout@v4`, `actions/setup-node@v4` 정확
- `gh pr comment` 명령어 정확
- Anthropic API 스트리밍 예시 코드 정확
- `@anthropic-ai/sdk` 패키지명 정확
- `anthropic.messages.stream()` API 정확
- `https://modelcontextprotocol.io/docs` URL 정확
- cron 문법 `'0 9 * * 1'` 매주 월요일 9시 정확
- LangGraph, AutoGen, CrewAI 프레임워크 특징 개략적 정확
- `npm audit --json` 활용 방식 정확
- CLAUDE.md 레이어 구조 설계 정확

## R1 대비 개선도
- R1 WHI: 0.22 (중간) — 심각 2개, 주의 5개, 검증항목 약 18개
- R2 WHI: 0.16 (낮음) — 심각 2개, 주의 3개, 검증항목 약 18개 (오타 수정으로 주의 1개 감소, 프레임워크 항목 재평가로 1개 감소)
- 개선율: 약 27%

## 전체 신뢰도 점수 (R2)
- **점수**: 7.8/10
- **근거**: 오타는 수정되었으나 교재명 오기재와 `--headless` 미존재 옵션이 잔존한다. 특히 `--headless` 옵션은 GitHub Actions 워크플로우 코드에 직접 포함되어 있어 독자가 그대로 사용할 경우 CI 파이프라인이 동작하지 않는다. 고수 기법과 팀 협업 방법의 전반적인 내용은 정확하고 실용적이다.
