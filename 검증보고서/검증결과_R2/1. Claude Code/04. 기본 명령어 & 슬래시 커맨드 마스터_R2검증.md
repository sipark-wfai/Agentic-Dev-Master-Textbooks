# 04. 기본 명령어 & 슬래시 커맨드 마스터 — 2차 할루시네이션 검증 보고서 (R2)

**검증일**: 2026-06-05
**검증 라운드**: Round 2
**원본 파일**: 04. 기본 명령어 & 슬래시 커맨드 마스터.md
**R1 수정 적용 상태**: 완료
**전체 할루시네이션 위험도**: 중간

## R1 수정 확인 결과
| 수정 항목 | 적용 여부 | 비고 |
|---------|---------|-----|
| 비존재 옵션 `--headless`, `--timeout` 제거 | 적용 확인 | 원문에 없음 |
| 비존재 슬래시 커맨드 `/status`, `/bug`, `/tools`, `/cd`, `/history` 제거 | 적용 확인 | 원문에 없음 |

## 잔존/신규 발견 오류

### 🔴 심각
| 항목 | 원문 내용 | 실제 사실 | 위치 |
|------|----------|----------|------|
| `--headless` 옵션 | "PR 자동 리뷰" 스크립트에서 `claude --headless --print` 사용 | `--headless` 옵션은 공식 Claude Code CLI에 존재하지 않음. `--print` 단독으로 사용해야 함 | 7.3절 세션 재개 활용, 부록 빠른 참조 카드 |
| `--print` vs `--headless --print` | 여러 곳에서 혼용 | `--print`만 정확한 옵션. `--headless`는 잘못된 옵션명 | 6절 시나리오 10 |

### 🟡 주의
| 항목 | 원문 내용 | 실제 사실/우려사항 | 위치 |
|------|----------|------------------|------|
| 모델명 `claude-opus-4-5` | 표 및 CLI 예제에서 사용 | 2026년 6월 기준 공식 모델명 확인 필요. claude-opus-4 또는 다른 버전일 수 있음 | 2.3절 모델 선택 |
| 모델명 `claude-sonnet-4-5` | 표 및 CLI 예제에서 사용 | 동일. claude-sonnet-4-5 공식 확인 필요 | 2.3절 |
| 모델명 `claude-haiku-4-5` | 표 및 CLI 예제에서 사용 | 동일. claude-haiku-4-5 공식 확인 필요 | 2.3절 |
| `/review` 슬래시 커맨드 | `/review`, `/review @파일` | Claude Code의 실제 슬래시 커맨드인지 불명확. 공식 문서에 없을 수 있음 | 3.6절 |
| `/vim` 슬래시 커맨드 | `/vim`, `/vim off` | 실제 지원 여부 불명확 | 3.7절 |
| `--allowedTools` 플래그 | `--allowedTools "Read,Write,Edit"` | 실제 플래그명 및 도구명이 정확한지 확인 필요. 공식 문서와 다를 수 있음 | 2.6절 |
| `--disallowedTools` | `--disallowedTools "Bash"` | 동일 | 2.6절 |
| `/permissions` 슬래시 커맨드 | "허가 설정 확인" | 실제 지원 여부 불명확 | 3.9절 |

### 🟢 확인됨 (정확)
- `claude`, `claude --print`, `claude --resume` 기본 옵션 정확
- Shift+Enter 멀티라인 입력 정확
- `@파일경로`, `@폴더경로` 컨텍스트 지시어 정확
- `/help`, `/clear`, `/compact`, `/cost`, `/memory` 슬래시 커맨드 정확
- Ctrl+C 작업 중단 단축키 정확

## R1 대비 개선도
- R1 WHI: 0.42 (높음 — 비존재 옵션 다수)
- R2 WHI: 0.28 (중간 — `--headless` 잔존, 모델명 불확실)
- 개선율: 33%

## 전체 신뢰도 점수 (R2)
- **점수**: 7.2/10
- **근거**: 1차에서 다수 옵션이 제거됐으나 `--headless`가 잔존하고 일부 슬래시 커맨드 및 모델명의 정확성이 여전히 불확실
