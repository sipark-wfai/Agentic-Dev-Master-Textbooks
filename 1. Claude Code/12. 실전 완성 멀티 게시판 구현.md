<!-- R4 수정: 2026-06-05 웹검색 기반 검증 완료 -->

# 12. 실전 완성 — SvelteKit + NestJS + PostgreSQL 16 멀티 게시판 구현

- **시리즈명**: Claude Code로 시작하는 에이전틱 개발 마스터
- **대상**: 대학생, 신입 개발자·기획자·디자이너
- **교재 명**: `12. 실전 완성 — SvelteKit + NestJS + PostgreSQL 16 멀티 게시판 구현`
- **최종 수정**: 2026-06-05
- **작성**: 워크포스에이아이(WorkforceAI)
- **협업**: Claude Sonnet 4.6
- **문의 이메일**: sipark@workforceai.kr

---

## 학습 목표

- SvelteKit 프론트엔드와 NestJS 백엔드를 연결하는 완전한 멀티 게시판 서비스를 구축한다.
- JWT 인증 흐름을 프론트엔드에서 구현한다.
- 동적 라우팅으로 8종 게시판을 분기 처리한다.
- Docker Compose로 전체 서비스를 로컬에서 실행한다.
- 실제 동작하는 게시판 서비스를 완성한다.

---

## 1. 모노레포 구조 전체 설계

### 1.1 pnpm 워크스페이스 설정

```yaml
# pnpm-workspace.yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

```json
// package.json (루트)
{
  "name": "board-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --filter './apps/*' build",
    "test": "pnpm --filter './apps/*' test",
    "lint": "pnpm --filter './apps/*' lint",
    "format": "pnpm --filter './apps/*' format",
    "db:migrate": "cd prisma && npx prisma migrate dev",
    "db:seed": "cd prisma && npx prisma db seed",
    "db:studio": "npx prisma studio"
  }
}
```

### 1.2 packages/shared 타입 정의

```typescript
// packages/shared/src/types.ts
export enum BoardType {
  GENERAL = 'GENERAL',
  GALLERY = 'GALLERY',
  VIDEO = 'VIDEO',
  QNA = 'QNA',
  NOTICE = 'NOTICE',
  FAQ = 'FAQ',
  INQUIRY = 'INQUIRY',
  FREE = 'FREE',
}

export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export interface User {
  id: string;
  email: string;
  nickname: string;
  role: Role;
}

export interface Board {
  id: string;
  name: string;
  slug: string;
  boardType: BoardType;
  description?: string;
  sortOrder: number;
}

export interface Post {
  id: string;
  title: string;
  content: string;
  viewCount: number;
  isAnonymous: boolean;
  isPinned: boolean;
  author: { id: string | null; nickname: string };
  board: { id: string; name: string; boardType: BoardType };
  commentCount: number;
  likeCount: number;
  createdAt: string;
}

export interface ApiResponse<T> {
  items: T[];
  meta: {
    total: number;
    page: number;
    limit: number;
    totalPages: number;
  };
}
```

---

## 2. 프론트엔드 (SvelteKit) 구현

### 2.1 SvelteKit 초기 설정

```bash
# SvelteKit 앱 생성
cd apps
# R3 수정: Svelte 5 기준 현재 권장 방식: npx sv create frontend
# 구버전(Svelte 4 기준): npm create svelte@latest frontend
npm create svelte@latest frontend
# TypeScript, ESLint, Prettier 선택

cd frontend
pnpm install
pnpm add tailwindcss @tailwindcss/typography
pnpm add lucide-svelte
```

### 2.2 JWT 인증 흐름

```typescript
// apps/frontend/src/lib/stores/auth.ts
import { writable, derived } from 'svelte/store';
import type { User } from '@board-hub/shared';

interface AuthState {
  user: User | null;
  accessToken: string | null;
  isLoading: boolean;
}

function createAuthStore() {
  const { subscribe, set, update } = writable<AuthState>({
    user: null,
    accessToken: null,
    isLoading: true,
  });

  return {
    subscribe,
    
    async login(email: string, password: string) {
      const response = await fetch('/api/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email, password }),
        credentials: 'include', // refreshToken 쿠키 수신
      });

      if (!response.ok) {
        const error = await response.json();
        throw new Error(error.message);
      }

      const data = await response.json();
      
      update(state => ({
        ...state,
        user: data.user,
        accessToken: data.accessToken,
      }));

      return data;
    },

    async logout() {
      await fetch('/api/auth/logout', {
        method: 'POST',
        credentials: 'include',
      });
      
      set({ user: null, accessToken: null, isLoading: false });
    },

    async refreshToken() {
      try {
        const response = await fetch('/api/auth/refresh', {
          method: 'POST',
          credentials: 'include',
        });

        if (!response.ok) throw new Error('Token refresh failed');

        const data = await response.json();
        update(state => ({ ...state, accessToken: data.accessToken }));
        return data.accessToken;
      } catch {
        set({ user: null, accessToken: null, isLoading: false });
        throw new Error('Session expired');
      }
    },

    setLoading(isLoading: boolean) {
      update(state => ({ ...state, isLoading }));
    },
  };
}

export const auth = createAuthStore();
export const isAuthenticated = derived(auth, $auth => $auth.user !== null);
export const isAdmin = derived(auth, $auth => $auth.user?.role === 'ADMIN');
```

### 2.3 API 클라이언트 설정

```typescript
// apps/frontend/src/lib/api/client.ts
import { auth } from '$lib/stores/auth';
import { get } from 'svelte/store';

const BASE_URL = '/api';

export async function apiRequest<T>(
  endpoint: string,
  options: RequestInit = {},
): Promise<T> {
  const { accessToken } = get(auth);
  
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(options.headers as Record<string, string>),
  };

  if (accessToken) {
    headers['Authorization'] = `Bearer ${accessToken}`;
  }

  let response = await fetch(`${BASE_URL}${endpoint}`, {
    ...options,
    headers,
    credentials: 'include',
  });

  // 401 응답 시 토큰 갱신 시도
  if (response.status === 401 && accessToken) {
    try {
      const newToken = await auth.refreshToken();
      headers['Authorization'] = `Bearer ${newToken}`;
      
      response = await fetch(`${BASE_URL}${endpoint}`, {
        ...options,
        headers,
        credentials: 'include',
      });
    } catch {
      throw new Error('인증이 필요합니다. 다시 로그인해주세요.');
    }
  }

  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: '오류가 발생했습니다' }));
    throw new Error(error.message);
  }

  if (response.status === 204) return undefined as T;
  return response.json();
}
```

### 2.4 게시판 목록 API

```typescript
// apps/frontend/src/lib/api/boards.ts
import type { Board, Post, ApiResponse } from '@board-hub/shared';
import { apiRequest } from './client';

export const boardsApi = {
  list: () => apiRequest<Board[]>('/boards'),
  
  posts: (boardId: string, params?: {
    page?: number;
    limit?: number;
    sort?: 'latest' | 'popular';
    search?: string;
  }) => {
    const query = new URLSearchParams();
    if (params?.page) query.set('page', String(params.page));
    if (params?.limit) query.set('limit', String(params.limit));
    if (params?.sort) query.set('sort', params.sort);
    if (params?.search) query.set('search', params.search);
    
    return apiRequest<ApiResponse<Post>>(`/boards/${boardId}/posts?${query}`);
  },
  
  getPost: (boardId: string, postId: string) =>
    apiRequest<Post>(`/boards/${boardId}/posts/${postId}`),
  
  createPost: (boardId: string, data: {
    title: string;
    content: string;
    isAnonymous?: boolean;
    fileIds?: string[];
    tags?: string[];
  }) => apiRequest<Post>(`/boards/${boardId}/posts`, {
    method: 'POST',
    body: JSON.stringify(data),
  }),
  
  deletePost: (boardId: string, postId: string) =>
    apiRequest<void>(`/boards/${boardId}/posts/${postId}`, { method: 'DELETE' }),
};
```

### 2.5 동적 라우팅: 게시판 목록 페이지

```typescript
// apps/frontend/src/routes/board/[board_id]/+page.ts
import type { PageLoad } from './$types';
import { boardsApi } from '$lib/api/boards';

export const load: PageLoad = async ({ params, url }) => {
  const page = Number(url.searchParams.get('page')) || 1;
  const sort = (url.searchParams.get('sort') as 'latest' | 'popular') || 'latest';
  const search = url.searchParams.get('search') || undefined;

  try {
    const [boards, postsData] = await Promise.all([
      boardsApi.list(),
      boardsApi.posts(params.board_id, { page, limit: 20, sort, search }),
    ]);

    const currentBoard = boards.find(b => b.id === params.board_id);
    if (!currentBoard) throw new Error('게시판을 찾을 수 없습니다');

    return {
      board: currentBoard,
      posts: postsData.items,
      meta: postsData.meta,
      boards,
    };
  } catch (error) {
    throw error;
  }
};
```

```svelte
<!-- apps/frontend/src/routes/board/[board_id]/+page.svelte -->
<script lang="ts">
  import type { PageData } from './$types';
  import { BoardType } from '@board-hub/shared';
  import PostCard from '$lib/components/PostCard.svelte';
  import Pagination from '$lib/components/Pagination.svelte';
  import BoardTypeIcon from '$lib/components/BoardTypeIcon.svelte';

  export let data: PageData;

  const { board, posts, meta } = data;
</script>

<svelte:head>
  <title>{board.name} - 게시판 허브</title>
</svelte:head>

<div class="container mx-auto px-4 py-6">
  <!-- 게시판 헤더 -->
  <div class="flex items-center gap-3 mb-6">
    <BoardTypeIcon boardType={board.boardType} class="w-8 h-8" />
    <div>
      <h1 class="text-2xl font-bold">{board.name}</h1>
      {#if board.description}
        <p class="text-gray-500 text-sm mt-1">{board.description}</p>
      {/if}
    </div>
  </div>

  <!-- 갤러리 게시판: 그리드 레이아웃 -->
  {#if board.boardType === BoardType.GALLERY}
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
      {#each posts as post (post.id)}
        <PostCard {post} variant="gallery" />
      {/each}
    </div>

  <!-- FAQ 게시판: 아코디언 레이아웃 -->
  {:else if board.boardType === BoardType.FAQ}
    <div class="space-y-2">
      {#each posts as post (post.id)}
        <PostCard {post} variant="faq" />
      {/each}
    </div>

  <!-- 기본 게시판: 테이블 레이아웃 -->
  {:else}
    <div class="bg-white rounded-lg border">
      {#each posts as post (post.id)}
        <PostCard {post} variant="list" />
      {/each}

      {#if posts.length === 0}
        <div class="text-center py-16 text-gray-400">
          <p>게시글이 없습니다.</p>
          <a href="/board/{board.id}/write" class="mt-4 inline-block text-blue-500 hover:underline">
            첫 번째 게시글을 작성해보세요
          </a>
        </div>
      {/if}
    </div>
  {/if}

  <!-- 페이지네이션 -->
  <Pagination {meta} class="mt-6" />
</div>
```

### 2.6 공통 컴포넌트: PostCard

```svelte
<!-- apps/frontend/src/lib/components/PostCard.svelte -->
<script lang="ts">
  import type { Post } from '@board-hub/shared';
  import { BoardType } from '@board-hub/shared';
  import { Eye, MessageSquare, Heart, Pin } from 'lucide-svelte';
  import { formatRelativeTime } from '$lib/utils/time';

  export let post: Post;
  export let variant: 'list' | 'gallery' | 'faq' = 'list';
</script>

{#if variant === 'gallery'}
  <!-- 갤러리 카드: 썸네일 중심 -->
  <a href="/board/{post.board.id}/{post.id}" class="block group">
    <div class="aspect-square bg-gray-100 rounded-lg overflow-hidden">
      <!-- R3 수정: post.thumbnail은 공유 타입 Post 인터페이스에 추가 필요: thumbnail?: string -->
      {#if post.thumbnail}
        <img src={post.thumbnail} alt={post.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform" />
      {:else}
        <div class="w-full h-full flex items-center justify-center text-gray-400">
          이미지 없음
        </div>
      {/if}
    </div>
    <p class="mt-2 text-sm font-medium truncate">{post.title}</p>
    <p class="text-xs text-gray-500">{post.author.nickname}</p>
  </a>

{:else if variant === 'faq'}
  <!-- FAQ 아코디언 -->
  <details class="border rounded-lg">
    <summary class="px-4 py-3 cursor-pointer font-medium hover:bg-gray-50">
      Q. {post.title}
    </summary>
    <div class="px-4 py-3 border-t bg-gray-50 text-gray-700">
      <!-- R3 수정: XSS 취약점. DOMPurify 등으로 sanitize 후 사용 필요: {@html DOMPurify.sanitize(post.content)} -->
      {@html post.content}
    </div>
  </details>

{:else}
  <!-- 기본 목록 형태 -->
  <a
    href="/board/{post.board.id}/{post.id}"
    class="flex items-center gap-4 px-4 py-3 border-b last:border-none hover:bg-gray-50 transition-colors"
  >
    {#if post.isPinned}
      <Pin size={14} class="text-blue-500 flex-shrink-0" />
    {/if}
    
    <div class="flex-1 min-w-0">
      <p class="font-medium truncate">
        {post.title}
      </p>
      <p class="text-sm text-gray-500 mt-0.5">
        {post.author.nickname} · {formatRelativeTime(post.createdAt)}
      </p>
    </div>
    
    <div class="flex items-center gap-3 text-sm text-gray-400 flex-shrink-0">
      <span class="flex items-center gap-1">
        <Eye size={14} />
        {post.viewCount.toLocaleString()}
      </span>
      <span class="flex items-center gap-1">
        <MessageSquare size={14} />
        {post.commentCount}
      </span>
      <span class="flex items-center gap-1">
        <Heart size={14} />
        {post.likeCount}
      </span>
    </div>
  </a>
{/if}
```

---

## 3. 백엔드 (NestJS) 핵심 설정

### 3.1 main.ts 설정

```typescript
// apps/backend/src/main.ts
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import helmet from 'helmet';
import * as cookieParser from 'cookie-parser';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // 보안 헤더
  app.use(helmet());
  
  // 쿠키 파서 (refreshToken용)
  app.use(cookieParser());

  // CORS 설정
  app.enableCors({
    origin: process.env.FRONTEND_URL || 'http://localhost:5173',
    credentials: true,
    methods: ['GET', 'POST', 'PATCH', 'DELETE', 'PUT'],
    allowedHeaders: ['Content-Type', 'Authorization'],
  });

  // 전역 DTO 유효성 검사
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      forbidNonWhitelisted: true,
      transform: true,
    }),
  );

  // API 접두사
  app.setGlobalPrefix('api/v1');

  const port = process.env.APP_PORT || 3000;
  await app.listen(port);
  console.log(`Server running on http://localhost:${port}`);
}

bootstrap();
```

### 3.2 JWT Guard 설정

```typescript
// apps/backend/src/auth/guards/jwt-auth.guard.ts
import { Injectable, ExecutionContext, UnauthorizedException } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from '../decorators/public.decorator';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private reflector: Reflector) {
    super();
  }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);

    if (isPublic) return true;

    return super.canActivate(context);
  }

  handleRequest(err: Error | null, user: unknown) {
    if (err || !user) {
      throw err || new UnauthorizedException('인증이 필요합니다');
    }
    return user;
  }
}
```

### 3.3 Multer 파일 업로드 설정

```typescript
// apps/backend/src/files/files.module.ts
import { Module } from '@nestjs/common';
import { MulterModule } from '@nestjs/platform-express';
import { diskStorage } from 'multer';
import { extname, join } from 'path';
import { v4 as uuidv4 } from 'uuid';
import { FilesController } from './files.controller';
import { FilesService } from './files.service';

const ALLOWED_MIME_TYPES = {
  image: ['image/jpeg', 'image/png', 'image/gif', 'image/webp'],
  video: ['video/mp4', 'video/webm'],
  document: ['application/pdf', 'text/plain'],
};

@Module({
  imports: [
    MulterModule.register({
      storage: diskStorage({
        destination: (req, file, cb) => {
          const now = new Date();
          const dir = join(
            process.cwd(),
            'uploads',
            String(now.getFullYear()),
            String(now.getMonth() + 1).padStart(2, '0'),
          );
          // R3 수정: require()는 CommonJS 방식. NestJS TypeScript/ESM 환경에서는 아래 방식 사용
          // import { mkdirSync } from 'fs'; 를 파일 상단에 추가 후
          // mkdirSync(dir, { recursive: true });
          require('fs').mkdirSync(dir, { recursive: true }); // TODO: ESM 방식으로 교체 필요
          cb(null, dir);
        },
        filename: (req, file, cb) => {
          const ext = extname(file.originalname);
          cb(null, `${uuidv4()}${ext}`);
        },
      }),
      fileFilter: (req, file, cb) => {
        const type = req.body.type || 'image';
        const allowed = ALLOWED_MIME_TYPES[type as keyof typeof ALLOWED_MIME_TYPES] || [];
        
        if (allowed.includes(file.mimetype)) {
          cb(null, true);
        } else {
          cb(new Error(`허용되지 않는 파일 형식: ${file.mimetype}`), false);
        }
      },
      limits: {
        fileSize: 500 * 1024 * 1024, // 500MB
      },
    }),
  ],
  controllers: [FilesController],
  providers: [FilesService],
  exports: [FilesService],
})
export class FilesModule {}
```

---

## 4. 데이터베이스 (PostgreSQL 16)

### 4.1 성능 인덱스 설계

```prisma
// prisma/schema.prisma 인덱스 추가 부분

model Post {
  // ... 필드들
  
  // 게시판별 최신순 조회 (가장 많이 사용)
  @@index([boardId, createdAt(sort: Desc)])
  // 게시판별 인기순 조회
  @@index([boardId, viewCount(sort: Desc)])
  // 작성자별 게시글 조회
  @@index([authorId])
  // Soft delete 최적화
  @@index([deletedAt])
  // Full-text 검색
  @@index([title, content])
}

model Comment {
  // 게시글별 댓글 조회
  @@index([postId, createdAt(sort: Asc)])
  // 부모 댓글 조회
  @@index([parentId])
}
```

### 4.2 시드 데이터

```typescript
// prisma/seed.ts
import { PrismaClient, BoardType, Role } from '@prisma/client';
import * as bcrypt from 'bcrypt';

const prisma = new PrismaClient();

async function main() {
  // 관리자 계정 생성
  const adminPassword = await bcrypt.hash('Admin123!@#', 12);
  const admin = await prisma.user.upsert({
    where: { email: 'admin@boardhub.com' },
    update: {},
    create: {
      email: 'admin@boardhub.com',
      hashedPassword: adminPassword,
      nickname: '관리자',
      role: Role.ADMIN,
    },
  });

  // 8종 게시판 생성
  const boards = [
    { name: '일반 게시판', slug: 'general', boardType: BoardType.GENERAL, sortOrder: 1 },
    { name: '갤러리', slug: 'gallery', boardType: BoardType.GALLERY, sortOrder: 2 },
    { name: '동영상', slug: 'video', boardType: BoardType.VIDEO, sortOrder: 3 },
    { name: 'Q&A', slug: 'qna', boardType: BoardType.QNA, sortOrder: 4 },
    { name: '공지사항', slug: 'notice', boardType: BoardType.NOTICE, sortOrder: 5 },
    { name: 'FAQ', slug: 'faq', boardType: BoardType.FAQ, sortOrder: 6 },
    { name: '1:1 문의', slug: 'inquiry', boardType: BoardType.INQUIRY, sortOrder: 7 },
    { name: '자유 게시판', slug: 'free', boardType: BoardType.FREE, sortOrder: 8 },
  ];

  for (const board of boards) {
    await prisma.board.upsert({
      where: { slug: board.slug },
      update: {},
      create: { ...board, description: `${board.name}입니다.` },
    });
  }

  console.log('Seed data created:', { admin: admin.email, boards: boards.length });
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect());
```

---

## 5. Docker Compose 설정

```yaml
# docker-compose.yml

services:
  postgres:
    image: postgres:16-alpine
    container_name: board_hub_db
    environment:
      POSTGRES_USER: boarduser
      POSTGRES_PASSWORD: boardpass
      POSTGRES_DB: board_hub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U boarduser -d board_hub"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: board_hub_backend
    environment:
      DATABASE_URL: postgresql://boarduser:boardpass@postgres:5432/board_hub
      JWT_SECRET: your-jwt-secret-here
      JWT_EXPIRES_IN: 1h
      JWT_REFRESH_SECRET: your-refresh-secret-here
      JWT_REFRESH_EXPIRES_IN: 30d
      NODE_ENV: production
      APP_PORT: 3000
      FRONTEND_URL: http://localhost:5173
    ports:
      - "3000:3000"
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    container_name: board_hub_frontend
    environment:
      PUBLIC_API_URL: http://backend:3000
    ports:
      - "5173:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
  uploads_data:
```

---

## 6. 로컬 구동 확인 절차

```bash
# Step 1: 환경 변수 설정
cp .env.example .env
# .env 파일에서 실제 값으로 변경

# Step 2: Docker로 PostgreSQL 시작
docker-compose up -d postgres

# Step 3: DB 마이그레이션 실행
npx prisma migrate dev --name init
npx prisma db seed

# Step 4: 개발 서버 시작
pnpm dev

# 출력 확인:
# frontend: ➜  Local: http://localhost:5173/
# backend: Server running on http://localhost:3000

# Step 5: API 테스트
curl http://localhost:3000/api/v1/boards
# 예상: [{id, name, boardType, ...} × 8]

# Step 6: 브라우저 확인
# http://localhost:5173 접속 -> 게시판 목록 확인
# 회원가입 -> 로그인 -> 게시글 작성 -> 댓글 작성
```

---

## 7. 정리 및 체크리스트

### 이 편에서 배운 내용

- pnpm 모노레포 구조와 shared 패키지 타입 공유
- SvelteKit JWT 인증 흐름 (로그인, 토큰 갱신, 자동 재인증)
- 동적 라우팅으로 8종 게시판 분기 처리
- NestJS 핵심 설정 (Helmet, CORS, 유효성 검사, JWT Guard)
- Multer 파일 업로드, Prisma 인덱스, Docker Compose

### 최종 체크리스트

- [ ] pnpm 모노레포가 올바르게 설정됐다
- [ ] 로그인/회원가입이 정상 동작한다
- [ ] 8종 게시판 목록이 각 레이아웃(리스트/갤러리/FAQ)으로 표시된다
- [ ] 게시글 작성/수정/삭제가 동작한다
- [ ] 파일 업로드가 동작한다
- [ ] Docker Compose로 전체 서비스가 실행된다

### 다음 편 예고

13편에서는 에이전트를 안전하게 제어하는 하네스 엔지니어링을 배운다. CLAUDE.md 규칙 강제, 파일 접근 제한, 인간 승인 Hook, 비용 제어 설정을 완성한다.

---

> **한줄 요약**: 완전히 동작하는 서비스는 프론트엔드-백엔드-DB가 유기적으로 연결돼야 한다. 공유 타입 패키지와 API 클라이언트 추상화가 전체 일관성을 보장한다.
