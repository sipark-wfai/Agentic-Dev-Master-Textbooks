<!-- R3 수정: Assistants API 버전 주의 주석 추가, LangGraph 버전 주의 주석 추가, amondnet/vercel-action 공식 대안 안내 추가 -->
# 15. ChatGPT Codex 고수되기 — 실무 팁·팀 협업·그 다음 단계

> **R6 수정**: Pro 2x 프로모 종료(2026-05-31) 비용 계획 업데이트, Windows Computer Use(2026-05-29) 신규 섹션 추가



> **학습 목표**
> - 고수의 태스크 설계 루틴과 사고방식을 습득한다
> - 비용 최소화 전략으로 효율적으로 Codex를 사용한다
> - 팀 협업에서 Codex를 도입하고 문화를 정착시키는 방법을 배운다
> - CI/CD 파이프라인을 완전 자동화한다
> - Codex 이후의 다음 학습 로드맵을 이해한다

---

## 1. 고수의 태스크 설계 루틴

### 1.1 초보와 고수의 차이

```
[초보의 태스크 설계]
"로그인 기능 만들어줘"
→ 결과: 불완전한 구현, 재실행 필요

[고수의 태스크 설계]
PRD 확인 → 범위 정의 → 의존성 파악 → 성공 기준 수립 → 태스크 작성
→ 결과: 첫 번에 완성도 높은 구현
```

### 1.2 고수의 태스크 설계 5단계

**1단계: PRD 기능 ID 확인**
```
구현할 기능의 PRD 기능 ID를 먼저 확인한다.
AUTH-002, POST-003 등 명확한 ID로 범위를 정한다.
```

**2단계: 의존성 파악**
```
이 태스크가 성공하려면 무엇이 먼저 있어야 하는가?
- AUTH-002 로그인 → JwtStrategy, PrismaService, UserModule이 있어야 함
```

**3단계: 파일 수 예측**
```
이 태스크에서 생성·수정될 파일이 10개 이하인가?
10개 초과라면 분할한다.
```

**4단계: 테스트 케이스 미리 작성**
```
PRD의 성공 기준을 테스트 케이스로 변환해둔다.
이를 태스크 지시문에 포함한다.
```

**5단계: 재사용 가능한 지시문 저장**
```
성공한 태스크 지시문을 템플릿으로 저장한다.
비슷한 태스크에서 재사용한다.
```

### 1.3 고수의 일일 루틴

```
09:00 오전 태스크 큐 설정
  - 밤 사이 완료된 PR 확인
  - 오늘 실행할 태스크 3~5개 준비
  - 우선순위 결정 (P0 → P1 → P2)

09:30 병렬 태스크 실행 (3개)
  - 의존성 없는 태스크 동시 실행
  - 큐에 넣고 다른 업무 시작

12:00 점심 전 PR 리뷰
  - 오전 태스크 완료 PR 확인
  - CI 통과 여부 확인
  - 문제없으면 Approve

13:00 오후 태스크 큐 설정
  - 오전 PR 머지 후 의존성 해제된 태스크 실행
  - 추가 병렬 태스크 3개 실행

17:00 저녁 PR 리뷰
  - 오후 태스크 완료 PR 확인
  - 내일을 위한 태스크 큐 준비

다음날 09:00: 반복
```

이 루틴으로 하루에 6~10개의 태스크를 처리할 수 있다.

---

> **[R5 업데이트 — 2026년 6월 1일 기준]** Pro 2x 프로모가 2026년 5월 31일 종료되었습니다. 기존 $100/월 Pro 5x 플랜 사용자는 6월 1일부터 실질 용량이 절반으로 줄어듭니다. 비용 계획 수립 시 이를 반영하세요. 또한 2026년 5월 29일부터 Windows에서도 Computer Use 기능이 지원됩니다(포어그라운드 전용, EEA·UK·스위스 미지원).

## 2. 비용 최소화 전략

### 2.1 모델 선택 최적화

```
┌─────────────────────────────────────────────────────┐
│             모델별 최적 사용 시나리오                  │
├──────────────────┬──────────────────────────────────┤
│    o4-mini       │    o3                             │
│    (비용 낮음)    │    (비용 높음)                    │
├──────────────────┼──────────────────────────────────┤
│ CRUD API 구현    │ 복잡한 알고리즘                   │
│ 테스트 작성      │ 보안 취약점 분석                  │
│ 문서 생성        │ 아키텍처 설계 검토                 │
│ 단순 버그 수정   │ 성능 최적화 전략                  │
│ DTO 작성         │ 레거시 코드 분석                  │
│ 스타일 수정      │ 복잡한 리팩터링                   │
└──────────────────┴──────────────────────────────────┘

80%의 태스크는 o4-mini로 처리 가능하다.
o3는 복잡한 판단이 필요한 20%에만 사용한다.
```

### 2.2 태스크 효율화 전략

**전략 1: AGENTS.md로 재실행 방지**

명확한 AGENTS.md가 있으면 첫 번에 올바른 코드가 생성된다.
재실행은 비용의 2배를 쓰는 것과 같다.

**전략 2: 지시문 템플릿 라이브러리 구축**

성공한 태스크 지시문을 팀 문서에 저장한다.

```markdown
# 태스크 지시문 라이브러리 (team-wiki/codex-templates.md)

## NestJS 모듈 생성 템플릿
[모듈명] 모듈을 apps/backend/src/[module]/에 생성해줘.
docs/API.md의 [섹션명] 스펙을 기반으로...

## SvelteKit 페이지 생성 템플릿
apps/frontend의 [경로] 페이지를 생성해줘.
+page.svelte와 +page.server.ts를 함께 작성...

## 테스트 추가 템플릿
[서비스파일]의 단위 테스트를 완성해줘.
Jest + @nestjs/testing 사용...
```

**전략 3: 배치 태스크**

여러 작은 작업을 하나의 태스크로 묶는다.

```
❌ 비효율:
태스크 1: "UserService에 JSDoc 추가"
태스크 2: "PostService에 JSDoc 추가"
태스크 3: "CommentService에 JSDoc 추가"

✅ 효율적:
태스크 1: "apps/backend/src/ 의 모든 Service 파일에 JSDoc 주석을 추가해줘.
           각 public 메서드에 @param, @returns, @throws 포함"
```

**전략 4: 재사용 가능한 AGENTS.md 패턴**

회사·팀별 공통 AGENTS.md 패턴을 만들어 새 프로젝트에 재사용한다.

```markdown
# AGENTS.md 패턴 라이브러리

## NestJS + PostgreSQL 패턴
[회사 표준 AGENTS.md 내용]

## SvelteKit 프론트엔드 패턴
[회사 표준 프론트엔드 AGENTS.md 내용]

## 새 프로젝트 시작 시:
1. 패턴 라이브러리에서 복사
2. 프로젝트별 커스터마이징
3. 팀과 공유하여 검토
```

---

## 3. 팀 협업에서 Codex 활용

### 3.1 팀 도입 단계

```
1단계: 파일럿 (1명)
  - 개인 프로젝트에서 먼저 학습
  - 성공 패턴과 실패 패턴 파악
  - 팀 내 Codex 가이드 작성

2단계: 소규모 도입 (2~3명)
  - 새 기능 개발에 Codex 적용
  - 팀 공유 AGENTS.md 작성
  - PR 리뷰 문화 정립 (Codex PR도 동일하게 리뷰)

3단계: 전체 팀 도입
  - 모든 이슈에 codex-ready 레이블 체계 적용
  - 태스크 분배 회의 (주간)
  - Codex PR 리뷰 담당자 로테이션

4단계: 프로세스 최적화
  - 태스크 지시문 라이브러리 구축
  - AGENTS.md 정기 업데이트 (월간)
  - 비용 모니터링 및 최적화
```

### 3.2 팀 공유 AGENTS.md 관리

```markdown
# 팀 AGENTS.md 관리 정책

## 소유권
AGENTS.md는 팀 전체의 합의로 변경한다.
개인이 임의로 수정하면 다른 팀원의 태스크에 영향을 줄 수 있다.

## 변경 절차
1. AGENTS.md 변경 PR 생성
2. 팀 리뷰 (최소 2명 Approve)
3. 다음 주 월요일에 머지 (주간 일정에 맞춤)
4. 변경 내용을 팀 Slack에 공유

## 정기 검토
- 월간: AGENTS.md 전체 내용 검토
  - 더 이상 유효하지 않은 규칙 제거
  - 새로운 패턴 추가
  - 기술 스택 버전 업데이트
```

### 3.3 역할별 태스크 분배

```
기획자/PM 역할:
- GitHub Issue 작성 (버그 리포트, 기능 요청)
- PRD 관리 및 업데이트
- Codex 태스크 결과 검토 (기능 동작 확인)

개발 리드 역할:
- AGENTS.md 설계 및 관리
- 복잡한 태스크 지시문 작성
- PR 최종 리뷰 및 머지 승인
- 아키텍처 결정

주니어 개발자 역할:
- 간단한 버그 수정 태스크 실행
- 테스트 작성 태스크 실행
- Codex PR 1차 리뷰
- 태스크 결과 문서화

QA 엔지니어 역할:
- E2E 테스트 시나리오 작성 (Playwright)
- 버그 이슈 상세 작성 (재현 방법 포함)
- 릴리즈 전 Codex PR 전체 검증
```

### 3.4 Codex PR 코드 리뷰 문화

Codex가 올린 PR을 잘못 다루면 문제가 발생한다.

```
잘못된 문화 ❌:
"Codex가 올린 거니까 믿고 그냥 머지"
→ 보안 취약점, 버그, PRD 불일치가 통과됨

올바른 문화 ✅:
"Codex PR도 사람 PR과 동일한 기준으로 리뷰"
→ CI 통과 확인
→ 코드 로직 검토
→ 테스트 의미 있는지 확인
→ 보안 취약점 없는지 확인
→ PRD 기능 ID와 일치하는지 확인
```

---

## 4. CI/CD 파이프라인 완전 자동화

### 4.1 완성된 자동화 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                완전 자동화 CI/CD 파이프라인                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  GitHub Issue 생성                                          │
│       ↓                                                     │
│  [codex-ready] 레이블 추가                                  │
│       ↓                                                     │
│  Codex 태스크 실행                                          │
│       ↓                                                     │
│  PR 생성 (codex/... 브랜치)                                 │
│       ↓                                                     │
│  GitHub Actions 자동 실행:                                   │
│  ┌──────────────────────────────┐                          │
│  │ 1. 빌드 테스트 (backend/frontend)                       │
│  │ 2. 단위 테스트 + 커버리지                                │
│  │ 3. E2E 테스트 (Playwright)                              │
│  │ 4. 보안 스캔 (npm audit)                                │
│  │ 5. 린트 검사                                            │
│  └──────────────────────────────┘                          │
│       ↓ 모두 통과                                           │
│  Slack/이메일 알림 → 리뷰어에게 알림                         │
│       ↓ 리뷰어 승인                                         │
│  develop 머지                                               │
│       ↓ (자동)                                              │
│  스테이징 환경 자동 배포                                     │
│       ↓ (수동 승인 후)                                      │
│  프로덕션 배포 (Vercel + Render)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Vercel 프론트엔드 자동 배포

```yaml
# .github/workflows/deploy-frontend.yml
name: Deploy Frontend to Vercel

on:
  push:
    branches: [ main ]
    paths: [ 'apps/frontend/**' ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter frontend build
      
      - name: Deploy to Vercel
        # R3 주의: amondnet/vercel-action은 커뮤니티 유지 액션으로 안정성이 낮을 수 있습니다.
        # Vercel 공식 배포 방법: vercel CLI를 직접 사용하거나 Vercel GitHub 통합을 이용하세요.
        # 공식 대안: https://vercel.com/docs/deployments/deploy-hooks
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID_FRONTEND }}
          working-directory: apps/frontend
          vercel-args: '--prod'
```

### 4.3 Render 백엔드 자동 배포

Render.com의 NestJS 백엔드 자동 배포 설정:

```yaml
# render.yaml (Render Infrastructure as Code)
services:
  - type: web
    name: board-hub-backend
    env: node
    region: oregon
    plan: starter
    buildCommand: |
      cd apps/backend
      pnpm install
      pnpm prisma migrate deploy
      pnpm build
    startCommand: |
      cd apps/backend
      node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: board-hub-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://board-hub.vercel.app

databases:
  - name: board-hub-db
    databaseName: board_hub
    user: boardhub
    plan: starter
```

### 4.4 Railway를 이용한 간편 배포

Railway는 Docker Compose를 그대로 배포할 수 있어 설정이 간단하다.

```toml
# railway.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/backend/Dockerfile"

[deploy]
startCommand = "node dist/main.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
```

```dockerfile
# apps/backend/Dockerfile
FROM node:20-alpine AS builder

WORKDIR /app
COPY package.json pnpm-workspace.yaml ./
COPY apps/backend/package.json ./apps/backend/
COPY packages/ ./packages/

RUN npm install -g pnpm
RUN pnpm install --frozen-lockfile

COPY apps/backend ./apps/backend
COPY prisma ./prisma

RUN pnpm --filter backend prisma generate
RUN pnpm --filter backend build

FROM node:20-alpine AS runner

WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/apps/backend/dist ./dist
COPY --from=builder /app/apps/backend/package.json ./
COPY --from=builder /app/prisma ./prisma

EXPOSE 3000
CMD ["node", "dist/main.js"]
```

---

## 5. ChatGPT Codex vs Claude Code 선택 기준

15편 시리즈의 마지막으로, 두 에이전트 도구의 선택 기준을 정리한다.

```
┌─────────────────────────────────────────────────────┐
│           Codex vs Claude Code 선택 기준             │
├───────────────────────┬─────────────────────────────┤
│    Codex 선택          │    Claude Code 선택          │
├───────────────────────┼─────────────────────────────┤
│ 비동기 실행이 중요할 때 │ 실시간 피드백이 필요할 때   │
│ 장시간 작업 (>10분)    │ 단시간 반복 작업 (<5분)     │
│ 여러 작업 병렬화 필요  │ 하나씩 순서대로 처리        │
│ GitHub 중심 워크플로우 │ 로컬 파일 직접 편집         │
│ 팀 협업, PR 기반 개발  │ 개인 프로젝트, 즉각 반영    │
│ 환경 독립성 필요       │ 로컬 환경에서 바로 테스트   │
│ 기획자·디자이너도 사용 │ 개발자 중심 사용            │
└───────────────────────┴─────────────────────────────┘

혼합 전략 (권장):
- 대형 기능 구현: Codex (비동기, 병렬)
- 세밀한 조정: Claude Code (실시간)
- 빠른 버그 수정: Claude Code
- 테스트 자동화: Codex (배치)
```

---

## 6. 이 시리즈 완주 후 다음 학습 로드맵

### 6.1 OpenAI API 직접 활용

Codex를 넘어 OpenAI API를 직접 사용하는 단계다.

```python
# OpenAI API 직접 호출 예시
from openai import OpenAI

client = OpenAI(api_key="your-api-key")

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[
        {"role": "system", "content": "You are a code review assistant"},
        {"role": "user", "content": "Review this code: ..."}
    ]
)
print(response.choices[0].message.content)
```

학습 자료:
- OpenAI Platform 문서 (platform.openai.com)
- OpenAI Cookbook (github.com/openai/openai-cookbook)

### 6.2 OpenAI Assistants API

Codex처럼 파일을 읽고 코드를 실행하는 에이전트를 직접 만드는 API다.

```python
# Assistants API 예시
# R3 주의: 아래 코드는 OpenAI Python SDK v1.x 기준입니다.
# Assistants API v2 출시 이후 일부 메서드 시그니처가 변경되었습니다.
# 사용 전 공식 문서(https://platform.openai.com/docs/assistants)에서 최신 API를 확인하세요.
assistant = client.beta.assistants.create(
    name="Code Reviewer",
    instructions="You are a code reviewer. Analyze code for bugs and security issues.",
    tools=[{"type": "code_interpreter"}, {"type": "file_search"}],
    model="gpt-4o",
)

thread = client.beta.threads.create()
message = client.beta.threads.messages.create(
    thread_id=thread.id,
    role="user",
    content="Review this NestJS service file",
    attachments=[{"file_id": file_id, "tools": [{"type": "file_search"}]}]
)

run = client.beta.threads.runs.create_and_poll(
    thread_id=thread.id,
    assistant_id=assistant.id,
)
```

### 6.3 LangChain·LangGraph 에이전트

더 복잡한 에이전트 워크플로우를 코드로 구성하는 프레임워크다.

```python
# LangGraph 에이전트 예시
# R3 주의: LangGraph는 버전 업데이트가 빠르며 API가 크게 변동될 수 있습니다.
# 아래 코드는 작성 시점(2025년) 기준이며, 최신 버전은 공식 문서(https://langchain-ai.github.io/langgraph/)를 확인하세요.
# 설치: pip install langgraph langchain-openai (버전 명시 권장)
from langgraph.graph import StateGraph, END
from langchain_openai import ChatOpenAI

# 상태 정의
class AgentState(TypedDict):
    task: str
    code: str
    test_result: str
    needs_revision: bool

# 노드 정의
def write_code(state: AgentState) -> AgentState:
    llm = ChatOpenAI(model="gpt-4o")
    code = llm.invoke(f"Write code for: {state['task']}")
    return {**state, "code": code.content}

def run_tests(state: AgentState) -> AgentState:
    # 테스트 실행 로직
    result = subprocess.run(["pytest", "-v"], capture_output=True, text=True)
    return {**state, "test_result": result.stdout, "needs_revision": result.returncode != 0}

# 그래프 구성
graph = StateGraph(AgentState)
graph.add_node("write_code", write_code)
graph.add_node("run_tests", run_tests)
graph.add_edge("write_code", "run_tests")
graph.add_conditional_edges("run_tests", lambda s: "write_code" if s["needs_revision"] else END)
```

### 6.4 추천 학습 경로

```
단계 1: ChatGPT Codex 마스터 (이 시리즈 완료 ✅)

단계 2: OpenAI API 기초 (1~2주)
  - API 키 발급 및 기본 호출
  - Chat Completions API
  - Embeddings API (벡터 검색)

단계 3: 프롬프트 엔지니어링 심화 (2~3주)
  - Few-shot prompting
  - Chain-of-thought
  - ReAct 패턴

단계 4: Assistants API (2주)
  - Thread, Run, Message 개념
  - Code Interpreter 활용
  - File Search 활용

단계 5: LangChain·LangGraph (3~4주)
  - Agent 기초
  - Tool 사용
  - Graph 기반 워크플로우

단계 6: 자체 에이전트 서비스 구축 (4~8주)
  - 도메인 특화 에이전트 설계
  - 프로덕션 배포 및 모니터링
  - 비용 최적화
```

---

## 7. 실무에서 검증된 고급 팁 모음

### 팁 1: 태스크 지시문의 "역할 부여" 기법

```
"당신은 시니어 NestJS 개발자입니다.
docs/PRD.md의 AUTH-002를 읽고 로그인 API를 구현해줘.
보안 취약점이 없는지 특히 주의해줘."

역할을 부여하면 더 신중하고 전문적인 코드가 생성된다.
```

### 팁 2: 단계별 실행 지시

```
"다음 단계로 실행해줘:
1단계: docs/ERD.md 읽기
2단계: 현재 prisma/schema.prisma 확인
3단계: 차이점 파악
4단계: 차이점만 수정

각 단계를 완료할 때마다 무엇을 했는지 로그에 출력해줘."
```

### 팁 3: 금지 사항으로 범위 제한

```
"게시물 목록 정렬 로직을 최적화해줘.

단, 다음은 하지 않는다:
- schema.prisma 수정 금지
- 마이그레이션 생성 금지
- PostController 수정 금지
- 새 패키지 설치 금지

PostService.findAll()만 최적화해줘."
```

### 팁 4: 예시 코드 제공

```
"apps/backend/src/user/user.service.ts를 참조하여
동일한 패턴으로 BoardService를 구현해줘.

특히 이 패턴을 그대로 따라줘:
async findById(id: number): Promise<Board> {
  const board = await this.prisma.board.findUnique({ where: { id } });
  if (!board) throw new NotFoundException('게시판을 찾을 수 없습니다');
  return board;
}
"
```

### 팁 5: 검증 단계 명시

```
"구현 완료 후 다음 검증을 실행해줘:
1. pnpm build (빌드 성공 확인)
2. pnpm test (모든 테스트 통과 확인)
3. pnpm lint (린트 오류 0개 확인)
4. pnpm -r exec tsc --noEmit (타입 오류 없음 확인)

4개 모두 성공한 후에만 PR을 생성해줘."
```

### 팁 6: 점진적 구현

```
"인증 모듈을 3단계로 나눠서 구현해줘.
각 단계마다 빌드 + 테스트 성공을 확인하고 진행해줘.

1단계: UserModule 기반 구조 (UserService, UserRepository)
2단계: AuthModule 핵심 (register, validateUser)
3단계: JWT 통합 (login, logout, JwtGuard)"
```

### 팁 7: 실패 허용 범위 명시

```
"다음 경우에는 태스크를 멈추고 보고해줘:
- 새 패키지 설치가 필요한 경우
- schema.prisma 변경이 필요한 경우
- 예상과 다른 코드 구조가 발견된 경우

위 경우가 아니면 판단하여 계속 진행해줘."
```

### 팁 8: 문서와 코드 동시 업데이트

```
"기능 구현 시 다음 문서도 함께 업데이트해줘:
- docs/API.md: 새 엔드포인트 추가
- apps/backend/src/[module]/README.md: 모듈 설명 업데이트
코드와 문서가 항상 동기화되도록 유지한다."
```

---

## 8. 15편 시리즈 완주 기념 — 최종 점검

이 시리즈를 완주했다면 다음을 확인해보자.

```
board-hub 프로젝트 완성도 체크리스트

인프라
✅ docker-compose.yml (PostgreSQL 16, Redis)
✅ prisma/schema.prisma (전체 스키마)
✅ prisma/migrations/ (초기 마이그레이션)
✅ .env.example (모든 환경 변수)
✅ .github/workflows/ (CI/CD)

백엔드 (NestJS)
✅ PrismaModule (@Global, Soft Delete 미들웨어)
✅ AuthModule (회원가입, 로그인, 로그아웃, 내 정보)
✅ UserModule
✅ BoardModule (CRUD, ADMIN 전용)
✅ PostModule (CRUD, 페이지네이션, 8종 타입별 특수 동작)
✅ CommentModule (CRUD, Q&A 채택)
✅ FileModule (이미지·동영상, 썸네일)
✅ 테스트 커버리지 70% 이상

프론트엔드 (SvelteKit)
✅ 레이아웃 (+layout.svelte, 인증 상태)
✅ 홈 페이지 (게시판 목록)
✅ 로그인/회원가입 페이지
✅ 게시판 목록 페이지 (타입별 레이아웃)
✅ 게시물 상세 페이지
✅ 게시물 작성 페이지
✅ 댓글 컴포넌트

문서
✅ AGENTS.md (루트, 백엔드, 프론트엔드)
✅ docs/PRD.md
✅ docs/ERD.md
✅ docs/API.md
✅ README.md

배포
✅ 프론트엔드: Vercel
✅ 백엔드: Render 또는 Railway
✅ DB: 서비스형 PostgreSQL
```

---

## 9. Codex 고수의 마인드셋

마지막으로, ChatGPT Codex를 진정한 생산성 도구로 만드는 마인드셋이다.

**"에이전트는 도구다, 판단은 인간이 한다"**

Codex는 강력하지만 실수한다. 환각, 보안 취약점, PRD 불일치 — 이 모든 것을 잡아내는 것이 개발자의 역할이다. PR 리뷰를 게을리하지 않는다.

**"작은 태스크가 좋은 코드를 만든다"**

거대한 태스크보다 작고 명확한 태스크가 더 좋은 결과를 낸다. 타임아웃도 없고, 충돌도 없고, 검토도 쉽다.

**"문서가 코드의 품질을 결정한다"**

AGENTS.md, PRD, ERD가 명확할수록 Codex의 결과물이 좋아진다. 문서에 투자하는 시간이 코드 수정에 드는 시간을 줄인다.

**"비동기를 두려워하지 않는다"**

Codex가 작업하는 동안 다른 일을 한다. 이것이 에이전트 시대의 멀티태스킹이다.

**"팀과 함께 성장한다"**

AGENTS.md를 팀이 함께 관리하고, PR 리뷰 문화를 함께 정립한다. Codex는 팀 도구다.


## Windows Computer Use (2026년 5월 29일)

2026년 5월 29일, v26.527 업데이트에서 Windows 플랫폼용 **Computer Use** 기능이 출시되었다. 이로써 Codex 에이전트가 Windows 데스크탑 애플리케이션과 직접 상호작용할 수 있게 되었다.

### Computer Use 특징 및 제한

```
✅ 지원:
  - Windows 데스크탑 앱 GUI 조작
  - 포어그라운드 앱 우선 실행
  - 스크린샷 기반 상태 확인

⚠️ 제한:
  - 포어그라운드(전면 활성) 앱에서만 동작
  - EEA(유럽경제지역), 영국, 스위스에서 미지원
  - macOS/Linux Computer Use와 동일 안전 정책 적용

❌ 금지:
  - 결제·송금 자동화
  - 보안 자격증명 입력 자동화
```

### 활용 시나리오

```
# Windows 전용 IDE에서 Codex 에이전트 활용
"Visual Studio에서 솔루션을 빌드하고 오류 목록을 캡처해줘"

# Windows 데스크탑 앱 테스트 자동화
"WPF 애플리케이션의 로그인 화면을 열고 테스트 자격증명으로 로그인 시도해줘"
```

> **참고**: Computer Use는 강력하지만 감독 없이 광범위한 시스템 접근을 허용하므로, 중요 작업 전에 반드시 내용을 검토한다.


---

> **한줄 요약**
> Codex 고수는 명확한 AGENTS.md와 설계 문서로 재실행을 방지하고, 비동기 병렬 태스크로 시간을 절약하며, 팀 공유 지시문 라이브러리로 일관된 코드를 유지하고, 모든 PR을 사람과 동일한 기준으로 리뷰하며, 다음 단계로 OpenAI API와 LangGraph를 향해 나아간다.

---

## 시리즈 완주를 축하한다

15편의 여정을 통해 ChatGPT Codex의 입문부터 실전 마스터까지 완주했다.

이 시리즈에서 배운 핵심:
1. Codex는 에이전틱 코딩 플랫폼이다 (1편)
2. GitHub 연동과 AGENTS.md가 기반이다 (2~3편)
3. 명확한 태스크 지시문이 성공을 결정한다 (4편)
4. GitHub와 CI/CD를 연동하면 자동화된다 (5편)
5. 비동기 병렬 실행으로 시간을 절약한다 (6편)
6. 샌드박스 환경을 이해하면 오류가 줄어든다 (7편)
7. 에이전트 친화적 PRD와 설계서가 품질을 결정한다 (8~9편)
8. PRD → 코드, 설계서 → 코드 전환 전략이 있다 (10~11편)
9. 실전 풀스택 구현은 분할하여 정복한다 (12편)
10. 품질 자동화로 기술 부채를 없앤다 (13편)
11. 20가지 오류를 알면 막힐 것이 없다 (14편)
12. 팀과 함께 성장하는 것이 진정한 마스터다 (15편)

다음 여정은 OpenAI API, Assistants API, LangGraph로 이어진다.


### 2026년 4월 과금 체계 전환

**토큰 기반 과금**으로 전환되어 실제 사용량에 따라 비용이 결정됩니다.



> **[R6 중요] Pro 프로모 종료 (2026년 5월 31일)**: $100/월 Pro 플랜의 2배 용량 프로모션이 2026년 5월 31일 종료되었다. 6월 1일부터 실제 사용 가능 용량이 절반으로 줄었다. 비용 계획 시 이를 반드시 반영하라. 현재 달 사용량 추이를 모니터링하고, 필요 시 $200/월 Pro 플랜으로 업그레이드를 검토한다.

| 항목 | 변경 내용 |
|------|----------|
| 과금 방식 | 구독 고정 → 토큰 기반 종량제 |
| 평균 비용 | 개발자당 $100~200/월 |
| Codex-only 시트 | 별도 ChatGPT 구독 없이 Codex만 사용 가능 |
| Pro 중간 티어 | $100/월 플랜 신설 |

`.codexignore` 파일로 불필요한 파일을 제외하여 토큰을 절약할 수 있습니다.


> **2026년 모델 업그레이드**: Codex는 GPT-5.5로 업그레이드되어 에이전틱 퍼스트 방식으로 훈련된 모델을 사용합니다. (2026년 4월 23일 전환)
> 2026년 6월 기준 사용 가능한 모델: **GPT-5.4**, **GPT-5.3-Codex** 등 다양한 버전이 제공됩니다.
> `/model` 명령으로 현재 사용 중인 모델을 확인하고 작업 유형에 맞게 전환하세요.
> - GPT-5.4: 범용 고성능 코딩
> - GPT-5.3-Codex: 코딩 특화, 빠른 응답


### 고급 CLI 명령 (v0.135+)

```bash
# 프로필 관리 (여러 프로젝트/조직 설정)
codex profiles

# 작업 히스토리 검색
codex history search "refactor"

# 환경 진단
codex doctor
```
