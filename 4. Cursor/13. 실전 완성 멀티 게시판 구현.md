> **R6 수정**: BugBot Autofix 수치 수정 (약 80% → PR merge rate 35% / 사용자 해결률 76%), Svelte 5 이벤트 문법 안내 강화 (🟡 R5검증 반영)

<!-- R6 수정: 2026-06-06 R5 검증보고서 반영 — BugBot 수치 정정, Svelte 5 문법 안내 -->

# 13. 실전 완성 멀티 게시판 구현

> **학습 목표**
> - 모노레포(pnpm workspace) 구조에서 SvelteKit + NestJS를 함께 개발한다
> - JWT 인증 스토어, 동적 라우팅, 파일 업로드 컴포넌트를 구현한다
> - NestJS 백엔드의 전체 모듈 구조를 이해한다
> - Docker Compose로 전체 스택을 로컬에서 실행한다

---

## 1. 모노레포 구조

### 1.1 최종 폴더 구조

```
multi-board-hub/
├── apps/
│   ├── frontend/
│   │   └── src/
│   │       ├── routes/
│   │       │   ├── +layout.svelte
│   │       │   ├── +page.svelte
│   │       │   ├── auth/
│   │       │   │   ├── login/+page.svelte
│   │       │   │   └── register/+page.svelte
│   │       │   └── board/
│   │       │       └── [board_id]/
│   │       │           ├── +page.svelte
│   │       │           ├── +page.server.ts
│   │       │           ├── new/+page.svelte
│   │       │           └── [post_id]/+page.svelte
│   │       └── lib/
│   │           ├── stores/auth.store.ts
│   │           ├── api/client.ts
│   │           └── components/
│   └── backend/
│       └── src/
│           ├── auth/
│           ├── board/
│           ├── post/
│           ├── comment/
│           └── file/
├── docs/
├── .cursor/rules/
├── docker-compose.yml
└── pnpm-workspace.yaml
```

---

## 2. SvelteKit 프론트엔드

### 2.1 JWT 인증 스토어

```typescript
// apps/frontend/src/lib/stores/auth.store.ts
import { writable, derived } from 'svelte/store';
import { browser } from '$app/environment';
import type { User } from '$lib/types';

interface AuthState {
  user: User | null;
  accessToken: string | null;
  isLoading: boolean;
}

function createAuthStore() {
  const { subscribe, set, update } = writable<AuthState>({
    user: null,
    accessToken: null,
    isLoading: true,
  });

  return {
    subscribe,

    setAuth(user: User, accessToken: string) {
      if (browser) {
        // ⚠️ [보안 경고] localStorage에 JWT 저장은 XSS 공격에 취약합니다.
        // 프로덕션에서는 반드시 httpOnly 쿠키 또는 메모리 저장 방식을 사용하세요.
        // 이 코드는 교육 목적의 단순화된 예시입니다.
        localStorage.setItem('accessToken', accessToken);
      }
      set({ user, accessToken, isLoading: false });
    },

    clearAuth() {
      if (browser) {
        localStorage.removeItem('accessToken');
      }
      set({ user: null, accessToken: null, isLoading: false });
    },

    async initialize() {
      if (!browser) {
        update(s => ({ ...s, isLoading: false }));
        return;
      }
      const savedToken = localStorage.getItem('accessToken');
      if (!savedToken) {
        update(s => ({ ...s, isLoading: false }));
        return;
      }
      try {
        const response = await fetch('/api/v1/auth/me', {
          headers: { Authorization: `Bearer ${savedToken}` },
        });
        if (response.ok) {
          const { data: user } = await response.json();
          set({ user, accessToken: savedToken, isLoading: false });
        } else {
          await this.refreshToken();
        }
      } catch {
        update(s => ({ ...s, isLoading: false }));
      }
    },

    async refreshToken(): Promise<boolean> {
      try {
        const response = await fetch('/api/v1/auth/refresh', {
          method: 'POST',
          credentials: 'include',
        });
        if (response.ok) {
          const { data } = await response.json();
          if (browser) localStorage.setItem('accessToken', data.accessToken);
          const meResponse = await fetch('/api/v1/auth/me', {
            headers: { Authorization: `Bearer ${data.accessToken}` },
          });
          if (meResponse.ok) {
            const { data: user } = await meResponse.json();
            set({ user, accessToken: data.accessToken, isLoading: false });
            return true;
          }
        }
      } catch { /* refresh 실패 */ }
      this.clearAuth();
      return false;
    },
  };
}

export const authStore = createAuthStore();
export const isLoggedIn = derived(authStore, ($auth) => !!$auth.user);
export const currentUser = derived(authStore, ($auth) => $auth.user);
```

### 2.2 API 클라이언트

```typescript
// apps/frontend/src/lib/api/client.ts
import { authStore } from '$lib/stores/auth.store';
import { get } from 'svelte/store';
import { goto } from '$app/navigation';

const API_BASE = 'http://localhost:3000/api/v1';

async function request<T>(endpoint: string, options: RequestInit & { skipAuth?: boolean } = {}): Promise<T> {
  const { skipAuth = false, ...fetchOptions } = options;
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(fetchOptions.headers as Record<string, string>),
  };

  if (!skipAuth) {
    const auth = get(authStore);
    if (auth.accessToken) headers['Authorization'] = `Bearer ${auth.accessToken}`;
  }

  const response = await fetch(`${API_BASE}${endpoint}`, {
    ...fetchOptions, headers, credentials: 'include',
  });

  if (response.status === 401 && !skipAuth) {
    const refreshed = await authStore.refreshToken();
    if (!refreshed) { goto('/auth/login'); throw new Error('인증이 만료되었습니다'); }
    const retryAuth = get(authStore);
    headers['Authorization'] = `Bearer ${retryAuth.accessToken}`;
    const retryResponse = await fetch(`${API_BASE}${endpoint}`, { ...fetchOptions, headers, credentials: 'include' });
    return retryResponse.json();
  }

  if (!response.ok) {
    const error = await response.json();
    throw new Error(error.message || '요청에 실패했습니다');
  }
  return response.json();
}

export const apiClient = {
  get: <T>(endpoint: string) => request<T>(endpoint, { method: 'GET' }),
  post: <T>(endpoint: string, body?: unknown) => request<T>(endpoint, { method: 'POST', body: JSON.stringify(body) }),
  patch: <T>(endpoint: string, body?: unknown) => request<T>(endpoint, { method: 'PATCH', body: JSON.stringify(body) }),
  delete: <T>(endpoint: string) => request<T>(endpoint, { method: 'DELETE' }),
};
```

### 2.3 동적 라우팅 — 게시글 목록 페이지

```typescript
// apps/frontend/src/routes/board/[board_id]/+page.server.ts
import type { PageServerLoad } from './$types';
import { error } from '@sveltejs/kit';

export const load: PageServerLoad = async ({ params, url, fetch }) => {
  const boardId = params.board_id;
  const page = Number(url.searchParams.get('page') || '1');
  const search = url.searchParams.get('search') || '';

  try {
    const [boardRes, postsRes] = await Promise.all([
      fetch(`http://localhost:3000/api/v1/boards/${boardId}`),
      fetch(`http://localhost:3000/api/v1/boards/${boardId}/posts?` +
        new URLSearchParams({ page: String(page), limit: '20', search })),
    ]);

    if (!boardRes.ok) throw error(404, '게시판을 찾을 수 없습니다');

    const { data: board } = await boardRes.json();
    const { data: posts, meta } = await postsRes.json();
    return { board, posts, meta, page, search };
  } catch (e) {
    throw error(500, '데이터를 불러오는 데 실패했습니다');
  }
};
```

```svelte
<!-- apps/frontend/src/routes/board/[board_id]/+page.svelte -->
<!-- ⚠️ Svelte 5 이벤트 문법: on:submit 대신 onsubmit={handler} 사용 -->
<script lang="ts">
  import type { PageData } from './$types';
  import PostList from '$lib/components/PostList.svelte';
  import { goto } from '$app/navigation';
  import { page } from '$app/stores';
  import { isLoggedIn } from '$lib/stores/auth.store';

  export let data: PageData;
  let searchQuery = data.search;

  function handleSearch() {
    const params = new URLSearchParams($page.url.searchParams);
    params.set('search', searchQuery);
    params.set('page', '1');
    goto(`?${params.toString()}`);
  }
</script>

<div class="max-w-4xl mx-auto px-4 py-8">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold">{data.board.name}</h1>
    {#if $isLoggedIn}
      <a href="/board/{data.board.id}/new" class="bg-blue-600 text-white px-4 py-2 rounded-lg">
        글쓰기
      </a>
    {/if}
  </div>

  <!-- Svelte 5: onsubmit={handler} -->
  <form onsubmit={handleSearch} class="mb-4 flex gap-2">
    <input bind:value={searchQuery} type="text" placeholder="검색어 입력"
      class="flex-1 border border-gray-300 rounded-lg px-3 py-2" />
    <button type="submit" class="bg-gray-800 text-white px-4 py-2 rounded-lg">검색</button>
  </form>

  <PostList posts={data.posts} boardType={data.board.type} />
</div>
```

---

## 3. NestJS 백엔드 모듈 구조

### 3.1 app.module.ts 전체 구성

```typescript
@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    PrismaModule,
    AuthModule,
    BoardModule,
    PostModule,
    CommentModule,
    FileModule,
  ],
})
export class AppModule {}
```

### 3.2 Multer 파일 업로드

```typescript
@Controller('files')
@UseGuards(JwtAuthGuard)
export class FileController {
  @Post('upload')
  @UseInterceptors(FileInterceptor('file'))
  async uploadFile(
    @UploadedFile() file: Express.Multer.File,
    @Body('postId') postId: string,
    @CurrentUser() user: User,
  ) {
    return this.fileService.upload(file, postId, user.id);
  }
}
```

---

## 4. Docker Compose 설정

```yaml
# docker-compose.yml
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: board_user
      POSTGRES_PASSWORD: board_password
      POSTGRES_DB: multi_board
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  postgres_data:
  minio_data:
```

### 4.1 로컬 구동 절차

```bash
docker-compose up -d           # 컨테이너 시작
cd apps/backend
pnpm prisma migrate dev        # DB 마이그레이션
pnpm start:dev                 # 백엔드 (http://localhost:3000/api/docs)
# 새 터미널
cd apps/frontend
pnpm dev                       # 프론트엔드 (http://localhost:5173)
```

---

## 5. BugBot으로 PR 자동 리뷰

### 5.1 BugBot 설정

```
Cursor Settings → Features → BugBot → Enable GitHub Integration

동작 방식:
1. GitHub PR 생성 시 BugBot 자동 분석
2. 버그·보안 취약점·성능 이슈 감지
3. Autofix 버튼 → 수정 커밋 자동 생성
```

### 5.2 BugBot Autofix 성능

<!-- R6 수정: BugBot 수치 정정 (R5 검증보고서 🟡 반영) -->

```
BugBot Autofix 공식 수치 (2026년 기준):
- PR merge rate: 약 35%
  (Autofix가 직접 PR 병합으로 이어지는 비율)
- 사용자 해결률(resolution rate): 약 76%
  (사용자가 수정 제안을 받아들여 문제를 해결하는 비율)

⚠️ 2026년 6월 이후 BugBot 갱신 시 usage-based billing으로 전환
```

---

## 체크리스트: 13편 학습 완료 확인

- [ ] pnpm workspace 모노레포 구조가 완성되었다
- [ ] authStore가 JWT 토큰을 관리하고 401 시 자동 refresh를 시도한다
- [ ] board/[board_id] 동적 라우팅이 작동한다
- [ ] Docker Compose로 PostgreSQL과 MinIO가 실행된다
- [ ] 백엔드 + 프론트엔드가 동시에 실행된다
- [ ] 회원가입 → 로그인 → 게시글 작성 → 조회 플로우가 작동한다
- [ ] BugBot Autofix의 정확한 수치(35% merge rate, 76% resolution rate)를 안다

---

> **한줄 요약**
> 멀티 게시판 허브는 SvelteKit + NestJS를 pnpm workspace 모노레포로 구성하고, JWT 인증 스토어·동적 라우팅·Multer 파일 업로드를 Cursor Agent로 구현하여 Docker Compose로 전체 스택을 실행한다. BugBot은 PR merge rate 35%, 사용자 해결률 76%로 자동 버그 수정을 지원한다.
