<!-- R4 수정: 2026-06-05 웹검색 기반 검증 완료 -->

# 11. PRD를 코드로

> **학습 목표**
> - PRD에서 코드로 변환하기 위한 준비 체크리스트를 완료한다
> - 마일스톤 기반 Agent 지시문 작성 전략을 익힌다
> - Agent 결과를 체계적으로 검토하고 수정하는 방법을 습득한다
> - 마일스톤 1(DB 스키마)과 마일스톤 2(인증 모듈)를 실제로 구현한다

---

## 1. 자율 구현 전 준비 체크리스트

### 1.1 필수 준비 항목

```
[환경 설정]
[ ] Node.js 20+ 설치 확인 (node --version)
[ ] pnpm 설치 확인 (pnpm --version)
[ ] Docker Desktop 실행 중
[ ] PostgreSQL 컨테이너 실행 중

[프로젝트 구조]
[ ] pnpm workspace 초기화 완료
[ ] apps/backend/ 폴더 생성
[ ] apps/frontend/ 폴더 생성
[ ] .env 파일 생성 (DATABASE_URL 포함)

[Cursor 설정]
[ ] .cursor/rules 작성 완료
[ ] docs/prd.md 작성 완료 (09편)
[ ] docs/erd.md 작성 완료 (10편)
[ ] docs/api.md 작성 완료 (10편)

[Git 초기화]
[ ] git init 완료
[ ] .gitignore 설정 (node_modules, .env, dist)
[ ] 초기 커밋 완료
```

### 1.2 프로젝트 초기 구조 생성

```bash
# 프로젝트 루트 초기화
mkdir multi-board-hub && cd multi-board-hub
git init
pnpm init

# workspace 구조 생성
mkdir -p apps/backend apps/frontend docs

# .gitignore
cat > .gitignore << 'EOF'
node_modules/
dist/
build/
.env
.env.local
*.log
coverage/
.DS_Store
EOF

# pnpm-workspace.yaml
cat > pnpm-workspace.yaml << 'EOF'
packages:
  - 'apps/*'
EOF

# NestJS 백엔드 초기화
cd apps/backend
npx @nestjs/cli new . --package-manager pnpm --skip-git

# SvelteKit 프론트엔드 초기화
cd ../frontend
pnpm create svelte@latest .  # <!-- R3 수정: 최신 방식은 `pnpm dlx sv create` 사용 권장. create svelte@latest도 동작함 -->
# TypeScript, ESLint, Prettier 선택
```

### 1.3 환경 변수 설정

```bash
# apps/backend/.env
DATABASE_URL="postgresql://board_user:board_password@localhost:5432/multi_board?schema=public"
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="your-refresh-token-secret-key-minimum-32-characters"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3000
NODE_ENV=development

# 파일 업로드 (MinIO 또는 AWS S3)
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="multi-board"

# apps/backend/.env.example (Git에 포함)
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
JWT_SECRET=""
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET=""
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3000
```

---

## 2. 마일스톤 분할 전략

### 2.1 마일스톤 전체 구조

```
마일스톤 1: 기반 인프라 (1일)
├── PostgreSQL + Docker 설정
├── Prisma 초기화
├── schema.prisma 전체 작성
└── 첫 번째 마이그레이션

마일스톤 2: 인증 모듈 (2일)
├── AuthModule 구조
├── 회원가입 API [AUTH-001]
├── 로그인 API [AUTH-002]
├── 토큰 재발급 [AUTH-003]
├── 로그아웃 [AUTH-004]
└── 내 정보 조회 [AUTH-005]

마일스톤 3: 게시판 + 게시글 CRUD (3일)
├── BoardModule (BOARD-001~005)
├── PostModule (POST-001~005)
└── Swagger 문서화

마일스톤 4: 댓글 + 파일 (2일)
├── CommentModule (COMMENT-001~004)
├── FileModule (FILE-001~002)
└── S3 업로드

마일스톤 5: 특수 기능 (2일)
├── QnA 채택 [POST-006]
├── 투표 기능 [POST-007]
└── 비밀게시판 접근 제어

마일스톤 6: 프론트엔드 (5일)
├── 레이아웃 + 라우팅
├── 인증 페이지
├── 게시판 목록
├── 게시글 CRUD
└── 댓글

마일스톤 7: 마무리 (2일)
├── 검색 기능
├── 성능 최적화
└── 배포 설정
```

---

## 3. Agent 지시문 작성 패턴

### 3.1 마일스톤 1 — DB 스키마 구현

```
[지시문]
Cmd+Shift+I → Composer → Agent 모드

"@Files docs/erd.md
@Files docs/prd.md

마일스톤 1: Prisma 스키마와 기반 인프라를 구성해줘.

작업 범위:
1. apps/backend/prisma/schema.prisma 작성
   - ERD에 있는 모든 모델 포함: User, Board, Post, Comment, File, RefreshToken, PollOption, PollVote
   - 소프트 삭제 (deletedAt) 모든 주요 모델에 적용
   - 인덱스: boardId, authorId, deletedAt, createdAt에 각각 적용
   - cascade delete: RefreshToken (User 삭제 시 함께 삭제)

2. apps/backend/src/prisma/prisma.service.ts 생성
   - PrismaClient 확장
   - onModuleInit에서 $connect
   - 소프트 삭제 미들웨어 (findMany, findFirst에서 deletedAt=null 자동 필터)
   - beforeExit hook

3. apps/backend/src/prisma/prisma.module.ts 생성
   - Global module로 설정 (@Global())
   - PrismaService export

4. apps/backend/src/app.module.ts 수정
   - PrismaModule import 추가

완료 기준:
- pnpm --filter backend build 에러 없음
- TypeScript 타입 에러 없음"
```

### 3.2 마일스톤 2 — 인증 모듈 구현

```
[지시문]
"@Files docs/prd.md
@Files apps/backend/prisma/schema.prisma
@Files apps/backend/src/prisma/

마일스톤 2: JWT 인증 모듈을 구현해줘.

요구사항 (PRD 기준):
- [AUTH-001] 회원가입: email, password(bcrypt 해시), name
- [AUTH-002] 로그인: accessToken(15분) + refreshToken(7일, DB 저장)
- [AUTH-003] 토큰 재발급: refreshToken으로 새 accessToken 발급
- [AUTH-004] 로그아웃: refreshToken DB에서 삭제
- [AUTH-005] 내 정보: 로그인한 유저 정보 반환

생성할 파일:
apps/backend/src/auth/
├── auth.module.ts
├── auth.controller.ts     (엔드포인트: /api/v1/auth/...)
├── auth.service.ts
├── strategies/
│   ├── jwt.strategy.ts    (accessToken 검증)
│   └── jwt-refresh.strategy.ts  (refreshToken 검증)
├── guards/
│   ├── jwt-auth.guard.ts
│   └── jwt-refresh.guard.ts
└── dto/
    ├── register.dto.ts
    └── login.dto.ts

설치 필요 패키지:
- @nestjs/jwt
- @nestjs/passport
- passport
- passport-jwt
- bcrypt
- @types/bcrypt
- @types/passport-jwt

완료 기준:
1. pnpm --filter backend build 에러 없음
2. 모든 DTO에 class-validator 데코레이터 있음
3. accessToken이 15분, refreshToken이 7일 만료
4. 로그인 성공 시 refreshToken DB에 저장됨
5. 로그아웃 시 refreshToken DB에서 삭제됨"
```

---

## 4. Agent 결과 검토 방법

### 4.1 변경 파일 목록 확인

```
Agent 완료 후 Composer 패널:
1. 변경된 파일 목록 확인
2. 예상하지 못한 파일이 수정되지 않았는지 확인
3. 필수 파일이 빠지지 않았는지 확인

예상 파일 목록 (마일스톤 2):
✓ apps/backend/src/auth/auth.module.ts
✓ apps/backend/src/auth/auth.controller.ts
✓ apps/backend/src/auth/auth.service.ts
✓ apps/backend/src/auth/strategies/jwt.strategy.ts
✓ apps/backend/src/auth/strategies/jwt-refresh.strategy.ts
✓ apps/backend/src/auth/guards/jwt-auth.guard.ts
✓ apps/backend/src/auth/guards/jwt-refresh.guard.ts
✓ apps/backend/src/auth/dto/register.dto.ts
✓ apps/backend/src/auth/dto/login.dto.ts
✓ apps/backend/src/app.module.ts  (AuthModule 등록)
✓ apps/backend/package.json  (새 패키지 추가)
```

### 4.2 코드 품질 검토 체크리스트

```typescript
// auth.service.ts 검토 포인트

// 1. bcrypt 사용 확인
import * as bcrypt from 'bcrypt';
const passwordHash = await bcrypt.hash(password, 12); // saltRounds=12

// 2. JWT 만료 시간 확인
const accessToken = this.jwtService.sign(payload, { expiresIn: '15m' });
const refreshToken = this.jwtService.sign(payload, { 
  secret: process.env.JWT_REFRESH_SECRET,
  expiresIn: '7d' 
});

// 3. refreshToken DB 저장 확인
await this.prisma.refreshToken.create({
  data: {
    token: refreshToken,
    userId: user.id,
    expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),
  },
});

// 4. 비밀번호 응답에서 제거 확인
const { passwordHash: _, ...userWithoutPassword } = user;
return { ...tokens, user: userWithoutPassword };

// 5. DTO 유효성 검사 확인
import { IsEmail, IsString, MinLength, MaxLength, Matches } from 'class-validator';

export class RegisterDto {
  @IsEmail({}, { message: '올바른 이메일 형식이 아닙니다' })
  email: string;

  @IsString()
  @MinLength(8, { message: '비밀번호는 8자 이상이어야 합니다' })
  @MaxLength(20, { message: '비밀번호는 20자를 초과할 수 없습니다' })
  @Matches(/^(?=.*[A-Za-z])(?=.*\d)(?=.*[@$!%*#?&])[A-Za-z\d@$!%*#?&]/, {
    message: '비밀번호는 영문, 숫자, 특수문자를 포함해야 합니다',
  })
  password: string;

  @IsString()
  @MinLength(2, { message: '이름은 2자 이상이어야 합니다' })
  @MaxLength(20, { message: '이름은 20자를 초과할 수 없습니다' })
  name: string;
}
```

### 4.3 빌드 및 타입 검증

```bash
# 백엔드 빌드 확인
cd apps/backend
pnpm build

# TypeScript 타입 체크
pnpm tsc --noEmit

# ESLint 검사
pnpm lint

# 빌드 성공 후 개발 서버 실행
pnpm start:dev

# API 테스트 (curl 또는 Swagger)
curl -X POST http://localhost:3000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"Test123!","name":"테스터"}'
```

### 4.4 Swagger 문서 확인

```typescript
// apps/backend/src/main.ts
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
import { ValidationPipe } from '@nestjs/common';
import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  
  // 전역 파이프 (유효성 검사)
  app.useGlobalPipes(new ValidationPipe({
    whitelist: true,
    forbidNonWhitelisted: true,
    transform: true,
  }));
  
  // API prefix
  app.setGlobalPrefix('api/v1');
  
  // CORS
  app.enableCors({
    origin: process.env.FRONTEND_URL || 'http://localhost:5173',
    credentials: true,
  });
  
  // Swagger
  const config = new DocumentBuilder()
    .setTitle('멀티 게시판 허브 API')
    .setDescription('멀티 게시판 허브 REST API 문서')
    .setVersion('1.0')
    .addBearerAuth()
    .build();
  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup('api/docs', app, document);
  
  await app.listen(process.env.PORT || 3000);
  console.log(`API: http://localhost:${process.env.PORT || 3000}/api/v1`);
  console.log(`Swagger: http://localhost:${process.env.PORT || 3000}/api/docs`);
}
bootstrap();
```

---

## 5. 마일스톤 1 실습: 완성된 코드 확인

### 5.1 schema.prisma 핵심 확인 포인트

```prisma
// apps/backend/prisma/schema.prisma

// 1. 모든 모델에 cuid() 기본값
model User {
  id String @id @default(cuid())
  // ...
}

// 2. 소프트 삭제 컬럼
  deletedAt DateTime?

// 3. 적절한 인덱스
  @@index([email])
  @@index([deletedAt])

// 4. 관계 cascade
model RefreshToken {
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

### 5.2 마이그레이션 실행

```bash
cd apps/backend

# 첫 마이그레이션
pnpm prisma migrate dev --name init_schema

# 마이그레이션 확인
pnpm prisma migrate status

# Prisma Studio로 DB 확인
pnpm prisma studio
# http://localhost:5555 에서 확인
```

---

## 6. 마일스톤 2 실습: 인증 모듈 핵심 코드

### 6.1 auth.service.ts 핵심 구현

```typescript
// apps/backend/src/auth/auth.service.ts
import { Injectable, ConflictException, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { PrismaService } from '../prisma/prisma.service';
import { RegisterDto } from './dto/register.dto';
import { LoginDto } from './dto/login.dto';
import * as bcrypt from 'bcrypt';

@Injectable()
export class AuthService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly jwtService: JwtService,
  ) {}

  /**
   * 회원가입
   * [AUTH-001] 이메일 중복 확인 후 비밀번호 해시 저장
   */
  async register(dto: RegisterDto) {
    const existing = await this.prisma.user.findUnique({
      where: { email: dto.email },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 이메일입니다');
    }

    const passwordHash = await bcrypt.hash(dto.password, 12);
    const user = await this.prisma.user.create({
      data: {
        email: dto.email,
        name: dto.name,
        passwordHash,
      },
    });

    const { passwordHash: _, ...result } = user;
    return result;
  }

  /**
   * 로그인
   * [AUTH-002] JWT accessToken + refreshToken 발급
   */
  async login(dto: LoginDto) {
    const user = await this.prisma.user.findUnique({
      where: { email: dto.email, deletedAt: null },
    });

    if (!user || !(await bcrypt.compare(dto.password, user.passwordHash))) {
      throw new UnauthorizedException('이메일 또는 비밀번호가 올바르지 않습니다');
    }

    const tokens = await this.generateTokens(user.id, user.email, user.role);
    await this.saveRefreshToken(user.id, tokens.refreshToken);

    const { passwordHash: _, ...userInfo } = user;
    return { ...tokens, user: userInfo };
  }

  private async generateTokens(userId: string, email: string, role: string) {
    const payload = { sub: userId, email, role };
    const [accessToken, refreshToken] = await Promise.all([
      this.jwtService.signAsync(payload, {
        secret: process.env.JWT_SECRET,
        expiresIn: process.env.JWT_EXPIRES_IN || '15m',
      }),
      this.jwtService.signAsync(payload, {
        secret: process.env.JWT_REFRESH_SECRET,
        expiresIn: process.env.JWT_REFRESH_EXPIRES_IN || '7d',
      }),
    ]);
    return { accessToken, refreshToken };
  }

  private async saveRefreshToken(userId: string, token: string) {
    const expiresAt = new Date();
    expiresAt.setDate(expiresAt.getDate() + 7);
    
    await this.prisma.refreshToken.create({
      data: { token, userId, expiresAt },
    });
  }

  /**
   * 로그아웃
   * [AUTH-004] refreshToken DB에서 삭제
   */
  async logout(refreshToken: string) {
    await this.prisma.refreshToken.deleteMany({
      where: { token: refreshToken },
    });
  }

  /**
   * 토큰 재발급
   * [AUTH-003] refreshToken 유효성 확인 후 새 accessToken 발급
   */
  async refreshTokens(userId: string, refreshToken: string) {
    const tokenRecord = await this.prisma.refreshToken.findFirst({
      where: {
        userId,
        token: refreshToken,
        expiresAt: { gt: new Date() },
      },
      include: { user: true },
    });

    if (!tokenRecord) {
      throw new UnauthorizedException('유효하지 않은 refresh token입니다');
    }

    const tokens = await this.generateTokens(
      tokenRecord.user.id,
      tokenRecord.user.email,
      tokenRecord.user.role,
    );
    
    // 기존 토큰 삭제 후 새 토큰 저장 (Refresh Token Rotation)
    await this.prisma.refreshToken.delete({ where: { id: tokenRecord.id } });
    await this.saveRefreshToken(userId, tokens.refreshToken);

    return tokens;
  }
}
```

---

## 7. Agent 실행 후 Git 커밋 전략

### 7.1 마일스톤별 커밋

```bash
# 마일스톤 1 완료 후
git add -A
git commit -m "feat(db): M1 - Prisma 스키마 및 마이그레이션 완료

- User, Board, Post, Comment, File, RefreshToken, PollOption, PollVote 모델 추가
- 소프트 삭제 (deletedAt) 전 모델 적용
- PrismaService 소프트 삭제 미들웨어 구현
- 초기 마이그레이션 실행 완료"

# 마일스톤 2 완료 후
git add -A
git commit -m "feat(auth): M2 - JWT 인증 모듈 완료

- 회원가입 [AUTH-001]: bcrypt 해시 저장
- 로그인 [AUTH-002]: accessToken(15m) + refreshToken(7d)
- 토큰 재발급 [AUTH-003]: Refresh Token Rotation
- 로그아웃 [AUTH-004]: refreshToken DB 삭제
- 내 정보 조회 [AUTH-005]
- JwtStrategy, JwtRefreshStrategy 구현
- class-validator DTO 유효성 검사 적용"
```

---

## 체크리스트: 11편 학습 완료 확인

- [ ] 자율 구현 전 준비 체크리스트를 모두 완료했다
- [ ] 마일스톤 1~7 분할 전략을 이해했다
- [ ] 마일스톤 1 지시문으로 schema.prisma를 Agent 생성했다
- [ ] 마이그레이션이 성공적으로 실행되었다
- [ ] 마일스톤 2 지시문으로 인증 모듈을 Agent 생성했다
- [ ] 코드 품질 검토 체크리스트를 통과했다
- [ ] pnpm build가 에러 없이 성공했다
- [ ] 회원가입 + 로그인 API를 Swagger에서 테스트했다

---

> **한줄 요약**
> PRD를 코드로 변환하는 핵심은 마일스톤 단위로 범위를 나누고, 각 마일스톤마다 PRD·ERD·완료 기준을 포함한 Agent 지시문을 작성하여 검토하는 반복 사이클이다.
